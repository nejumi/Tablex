# 0119 Current Verification Checkpoint

Date: 2026-07-06

This checkpoint records current evidence for `docs/exec-plans/0119_target_product_gap_closure_directive.md`.
It is intentionally not a completion declaration. The end-to-end product scenario remains unverified.

## Verified Commands

- `npm run build` in `apps/frontend`: passed after restoring the project top navigation to Home, Data, Insight, Leaderboard, Assets, and More; Vite still reports the existing large chunk warning.
- `uv run pytest apps/backend/tests -q`: `381 passed, 6 warnings` after the ResearchPlan open-deliverable preservation, model-diagnostics importance/PDP/SHAP updates, current-work summary validation, and Notebook index visual-story robustness update.
- `.venv/bin/python -m pytest apps/backend/tests/test_agent_sessions.py -q -k "notebook_file_request"`: `7 passed, 102 deselected`.
- `.venv/bin/pytest apps/backend/tests/test_agent_sessions.py -q -k "notebook_file_request or research_plan_file_requests_accept_explicit_legacy_path_and_node_alias"`: `9 passed, 105 deselected`.
- `.venv/bin/pytest apps/backend/tests/test_agent_sessions.py -q -k "experiment_result_file_request_registers_leaderboard_run_with_ack or pipeline_request_registers_prediction_pipeline_and_links_run or notebook_file_request_accepts_explicit_top_level_payload"`: `3 passed, 111 deselected`.
- `.venv/bin/pytest apps/backend/tests/test_agent_sessions.py apps/backend/tests/test_api_flow.py -q -k "experiment_result_request or pipeline_request or prediction_pipeline_worker or pipeline_bundle or pilot_phase_vertical"`: `16 passed, 221 deselected, 1 warning`.
- `.venv/bin/pytest apps/backend/tests/test_agent_sessions.py apps/backend/tests/test_api_flow.py -q -k "experiment_result_request or pipeline_request or prediction_pipeline_worker or pipeline_bundle or pilot_phase_vertical or current_work_nudge"`: `20 passed, 219 deselected, 1 warning`.
- `.venv/bin/python -m pytest apps/backend/tests/test_analysis_notebooks.py -q`: `25 passed`.
- `.venv/bin/python -m pytest apps/backend/tests/test_analysis_notebooks.py -q -k "static_html or native_marimo or notebook_index"`: `15 passed, 10 deselected`.
- `.venv/bin/python -m pytest apps/backend/tests/test_api_flow.py -q -k "native_marimo_runtime_error"`: `2 passed, 117 deselected, 1 warning`.
- `.venv/bin/python -m pytest apps/backend/tests/test_api_flow.py -q -k "native_marimo"`: `9 passed, 111 deselected, 1 warning`.
- `.venv/bin/python -m pytest apps/backend/tests/test_api_flow.py -q -k "native_marimo_open_rejects_static_html or native_marimo"`: `9 passed, 112 deselected, 1 warning`.
- `.venv/bin/python -m pytest apps/backend/tests/test_api_flow.py -q -k "session_data_access"`: `1 passed, 119 deselected, 1 warning`.
- `.venv/bin/python -m pytest apps/backend/tests/test_api_flow.py -q -k "attention"`: `4 passed, 117 deselected, 1 warning`.
- `.venv/bin/python -m pytest apps/backend/tests/test_agent_sessions.py -q -k "turn_start_silence or attention_chat_turn_dedupes"`: `2 passed, 107 deselected`.
- `.venv/bin/python -m pytest apps/backend/tests/test_agent_sessions.py -q -k "current_work_nudge or safe_nudge_requests_research_plan_current_work or safe_progress_update"`: `4 passed, 108 deselected`.
- `.venv/bin/python -m pytest apps/backend/tests/test_agent_sessions.py -q -k "research_plan_file_request or research_plan_tool or current_work or progress_update"`: `17 passed, 95 deselected`.
- `.venv/bin/pytest apps/backend/tests/test_agent_sessions.py -q -k "research_plan_file_request or failed_research_plan_file_request or current_work_nudge or safe_nudge_requests_research_plan_current_work"`: `8 passed, 105 deselected`.
- `.venv/bin/pytest apps/backend/tests/test_agent_sessions.py apps/backend/tests/test_agent_runner.py -q -k "prepare_session_workspace or workspace_python_shims or research_plan_file_request or failed_research_plan_file_request or current_work_nudge or safe_nudge_requests_research_plan_current_work"`: `10 passed, 112 deselected`.
- `.venv/bin/python -m pytest apps/backend/tests/test_api_flow.py -q -k "figure_rich_notebook"`: `1 passed, 118 deselected, 1 warning`.
- `.venv/bin/python -m pytest apps/backend/tests/test_api_flow.py -q -k "pilot_phase_vertical"`: `1 passed, 118 deselected, 1 warning`.
- `.venv/bin/python -m pytest apps/backend/tests/test_api_flow.py -q -k "pilot or pipeline_bundle"`: `2 passed, 117 deselected, 1 warning`.
- `.venv/bin/python -m pytest apps/backend/tests/test_research_plan_timeline.py -q`: `22 passed`.
- `.venv/bin/python -m pytest apps/backend/tests/test_agent_runner.py apps/backend/tests/test_config.py -q`: `11 passed`.
- `.venv/bin/python -m pytest apps/backend/tests/test_agent_sessions.py -q -k "pipeline_request or prediction_pipeline_worker"`: `7 passed, 102 deselected`.
- `.venv/bin/python -m pytest apps/backend/tests/test_agent_sessions.py -q -k "pilot_validation_audit or pilot_outcome_scoring_worker or prediction_pipeline_worker or pipeline_request"`: `10 passed, 99 deselected`.
- `.venv/bin/pytest apps/backend/tests/test_agent_sessions.py -q -k "same_research_plan_failure or research_plan_ingest_rejects_invalid_workspace_plan_without_canonical_revision or research_plan_file_requests_commit_presence_links_and_attention"`: `3 passed`.
- `.venv/bin/pytest apps/backend/tests/test_api_flow.py -q -k "notebook_versions_by_latest or latest_notebook_action_status or adjacent_notebook_updates"`: `3 passed`.
- `.venv/bin/pytest apps/backend/tests/test_agent_sessions.py -q -k "pending_research_plan_node or unknown_research_plan_node or experiment_result_file_request_registers_leaderboard_run_with_ack or experiment_result_request_links_runs_to_research_plan_node"`: `5 passed`.
- `.venv/bin/pytest apps/backend/tests/test_agent_sessions.py -q -k "pipeline_request_accepts_top_level_codex_aliases or pipeline_request_accepts_live_codex_manifest_compatibility or pipeline_request_registers_prediction_pipeline_and_links_run or pending_research_plan_node"`: `4 passed`.
- `.venv/bin/pytest apps/backend/tests/test_agent_sessions.py apps/backend/tests/test_api_flow.py -q -k "same_research_plan_failure or pending_research_plan_node or pipeline_request_accepts_top_level_codex_aliases or pipeline_request_accepts_live_codex_manifest_compatibility or pipeline_request_registers_prediction_pipeline_and_links_run or latest_notebook_action_status or adjacent_notebook_updates or notebook_versions_by_latest"`: `8 passed, 235 deselected, 1 warning`.
- `.venv/bin/pytest apps/backend/tests/test_agent_sessions.py -q -k "research_findings_request"`: `3 passed, 118 deselected`.
- `.venv/bin/pytest apps/backend/tests/test_agent_sessions.py apps/backend/tests/test_api_flow.py -q -k "research_findings_request or research_findings_json_preview or agent_activity_uses_session_attention_chat_turn_as_human_summary or agent_attention_chat_display_hides_internal_tool_terms"`: `6 passed, 238 deselected, 1 warning`.
- `.venv/bin/pytest apps/backend/tests/test_agent_sessions.py apps/backend/tests/test_api_flow.py -q -k "research_findings_request or research_findings_json_preview"`: `4 passed, 240 deselected, 1 warning`.
- `.venv/bin/pytest apps/backend/tests/test_api_flow.py -q -k "research_findings_json_preview or pilot_report_previews_hide_internal_ids or pilot_phase_vertical or pipeline_bundle"`: `4 passed, 120 deselected, 1 warning`.
- `.venv/bin/pytest apps/backend/tests/test_agent_sessions.py -q -k "structured_model_results or experiment_result_file_request or pipeline_registration or notebook_file_request or model_diagnostics_notebook_request_requires_diagnostic_manifest or model_context or quality_manifest"`: `19 passed, 105 deselected`.
- `.venv/bin/pytest apps/backend/tests/test_agent_sessions.py -q -k "structured_model_results_accept_runs_array or codex_structured_model_results_materialize or project_response_locale or experiment_result_file_request_registers_leaderboard_run_with_ack"`: `4 passed, 120 deselected`.
- `.venv/bin/pytest apps/backend/tests/test_agent_sessions.py -q -k "prepare_session_workspace or research_findings_request or structured_model_results or model_diagnostics_notebook_request_requires_diagnostic_manifest or notebook_file_request"`: `21 passed, 103 deselected`.
- `.venv/bin/pytest apps/backend/tests/test_agent_sessions.py -q -k "structured_model_results or experiment_result_file_request or pipeline_registration or notebook_file_request or model_diagnostics_notebook_request_requires_diagnostic_manifest or model_context or quality_manifest or prepare_session_workspace or research_findings_request"`: `23 passed, 101 deselected`.
- `.venv/bin/pytest apps/backend/tests/test_agent_sessions.py -q -k "research_findings_request_registers_report_evidence_and_plan_link or pilot_validation_audit_request_registers_artifact_evidence_and_plan_link or structured_model_results or experiment_result_file_request or model_diagnostics_notebook_request_requires_diagnostic_manifest"`: `12 passed, 112 deselected`.
- `.venv/bin/pytest apps/backend/tests/test_agent_sessions.py apps/backend/tests/test_research_plan_timeline.py -q -k "research_findings_request or pilot_validation_audit or pilot_outcome_scoring_worker or structured_model_results or experiment_result_file_request or model_diagnostics_notebook_request_requires_diagnostic_manifest or completion_evidence or registered_deliverables"`: `17 passed, 129 deselected`.
- `.venv/bin/pytest apps/backend/tests/test_agent_sessions.py apps/backend/tests/test_research_plan_timeline.py apps/backend/tests/test_api_flow.py -q -k "research_findings_request or pilot_validation_audit or pilot_outcome_scoring_worker or pilot_phase_vertical or structured_model_results or experiment_result_file_request or model_diagnostics_notebook_request_requires_diagnostic_manifest or completion_evidence or registered_deliverables"`: `18 passed, 252 deselected, 1 warning`.
- `.venv/bin/pytest apps/backend/tests/test_agent_sessions.py -q -k "prepare_session_workspace or turn_prompt_keeps_chat_update_human_facing_not_internal_changelog or model_diagnostics_notebook_request_requires_diagnostic_manifest or experiment_result_file_request"`: `5 passed, 119 deselected`.
- `.venv/bin/pytest apps/backend/tests/test_agent_sessions.py apps/backend/tests/test_research_plan_timeline.py apps/backend/tests/test_api_flow.py -q -k "research_findings_request or pilot_validation_audit or pilot_outcome_scoring_worker or pilot_phase_vertical or structured_model_results or experiment_result_file_request or model_diagnostics_notebook_request_requires_diagnostic_manifest or prepare_session_workspace or turn_prompt_keeps_chat_update_human_facing_not_internal_changelog or completion_evidence or registered_deliverables"`: `20 passed, 250 deselected, 1 warning`.
- `uv run pytest apps/backend/tests/test_model_diagnostics_artifacts.py -q`: `9 passed` after adding coverage that partial dependence follows supported native tree feature-importance APIs when available and permutation-importance ranking when native importance is unavailable.
- `.venv/bin/pytest apps/backend/tests/test_api_flow.py -q -k "project_upload_profile_evaluation_split_flow"`: `1 passed, 123 deselected, 3 warnings`.
- `.venv/bin/pytest apps/backend/tests/test_agent_sessions.py apps/backend/tests/test_research_plan_timeline.py apps/backend/tests/test_api_flow.py apps/backend/tests/test_model_diagnostics_artifacts.py -q -k "research_findings_request or pilot_validation_audit or pilot_outcome_scoring_worker or pilot_phase_vertical or structured_model_results or experiment_result_file_request or model_diagnostics_notebook_request_requires_diagnostic_manifest or prepare_session_workspace or turn_prompt_keeps_chat_update_human_facing_not_internal_changelog or project_upload_profile_evaluation_split_flow or partial_dependence or shap_summary or completion_evidence or registered_deliverables"`: `23 passed, 252 deselected, 3 warnings`.
- `.venv/bin/pytest apps/backend/tests/test_agent_sessions.py apps/backend/tests/test_research_plan_timeline.py apps/backend/tests/test_api_flow.py apps/backend/tests/test_model_diagnostics_artifacts.py -q -k "research_findings_request or pilot_validation_audit or pilot_outcome_scoring_worker or pilot_phase_vertical or structured_model_results or experiment_result_file_request or model_diagnostics_notebook_request_requires_diagnostic_manifest or prepare_session_workspace or turn_prompt_keeps_chat_update_human_facing_not_internal_changelog or project_upload_profile_evaluation_split_flow or partial_dependence or shap_summary or artifact_output_types or completion_evidence or registered_deliverables"`: `24 passed, 252 deselected, 3 warnings`.
- `.venv/bin/pytest apps/backend/tests/test_research_plan_timeline.py -q -k "initial_research_plan or harness_dataset_upload or harness_objective or static_html_as_notebook_deliverable or artifact_output_types or contract_validation"`: `7 passed, 17 deselected`.
- `.venv/bin/pytest apps/backend/tests/test_agent_sessions.py apps/backend/tests/test_research_plan_timeline.py apps/backend/tests/test_api_flow.py apps/backend/tests/test_model_diagnostics_artifacts.py -q -k "research_findings_request or pilot_validation_audit or pilot_outcome_scoring_worker or pilot_phase_vertical or structured_model_results or experiment_result_file_request or model_diagnostics_notebook_request_requires_diagnostic_manifest or prepare_session_workspace or turn_prompt_keeps_chat_update_human_facing_not_internal_changelog or project_upload_profile_evaluation_split_flow or partial_dependence or shap_summary or artifact_output_types or completion_evidence or registered_deliverables or initial_research_plan"`: `25 passed, 252 deselected, 3 warnings`.
- `.venv/bin/pytest apps/backend/tests/test_research_plan_timeline.py -q`: `24 passed`.
- `uv run pytest apps/backend/tests/test_agent_sessions.py apps/backend/tests/test_api_flow.py -q -k "research_findings_request or pilot_observation_followup or pilot_validation_audit or pilot_outcome_scoring_worker or pilot_phase_vertical"`: `8 passed, 241 deselected, 1 warning`.
- `.venv/bin/pytest apps/backend/tests/test_agent_sessions.py -q -k "research_plan_file_requests_commit_presence_links_and_attention or research_plan_file_requests_accept_explicit_legacy_path_and_node_alias or pending_research_plan_node"`: `3 passed, 122 deselected`.
- `.venv/bin/pytest apps/backend/tests/test_agent_sessions.py apps/backend/tests/test_research_plan_timeline.py apps/backend/tests/test_api_flow.py apps/backend/tests/test_model_diagnostics_artifacts.py -q -k "research_findings_request or pilot_observation_followup or pilot_validation_audit or pilot_outcome_scoring_worker or pilot_phase_vertical or structured_model_results or experiment_result_file_request or model_diagnostics_notebook_request_requires_diagnostic_manifest or prepare_session_workspace or turn_prompt_keeps_chat_update_human_facing_not_internal_changelog or project_upload_profile_evaluation_split_flow or partial_dependence or shap_summary or artifact_output_types or completion_evidence or registered_deliverables or initial_research_plan or research_plan_file_requests"`: `28 passed, 250 deselected, 3 warnings`.
- `.venv/bin/pytest apps/backend/tests/test_agent_sessions.py -q`: `125 passed`.
- `uv run pytest apps/backend/tests/test_analysis_notebooks.py -q`: `27 passed` after adding coverage that invalid native marimo source remains visible as a repair target instead of disappearing from the notebook index, and that a fixed `notebook_quality_manifest` drives notebook readiness display.
- `uv run pytest apps/backend/tests/test_research_plan_timeline.py -q`: `25 passed` after adding coverage that duplicate ResearchPlan artifact edges are deduped before artifact lookup.
- `uv run pytest apps/backend/tests/test_research_plan_timeline.py -q`: `27 passed` after adding coverage that an open ResearchPlan node with a fixed deliverable contract cannot disappear unless a replacement node carries the same expected output contract.
- `uv run pytest apps/backend/tests/test_agent_sessions.py -q`: `125 passed`.
- Seeded product-slice API checks for project `p_f6b8a035271c`: ResearchPlan contract validation `ok` with `next_iteration` current work paused; Notebook index has 1 data-understanding native source with `status=ready`, `native_marimo_status=source_registered`, and 2 declared figures; Leaderboard has 1 run with model description, features, and linked pipeline artifact `art_2c4cd975c82e`.
- Regression API check for project `p_82d0cabf4326`: `GET /api/projects/p_82d0cabf4326/research-plan/timeline?locale=ja-JP` returned HTTP `200`, contract validation `ok`, 7 blocks, and 60 artifact links after deduping/chunking ResearchPlan link id lookup.
- `POST /api/analysis-notebooks/art_1eece99336a8/marimo-session`: HTTP `200`, returned `status=running` and `runtime.has_error=false`.
- `.venv/bin/pytest apps/backend/tests/test_research_plan_timeline.py -q -k "artifact_output_types or registered_deliverables or completion_evidence"`: `2 passed, 21 deselected`.
- `.venv/bin/pytest apps/backend/tests/test_research_plan_timeline.py -q -k "static_html_as_notebook_deliverable or artifact_output_types or registered_deliverables or completion_evidence"`: `3 passed, 20 deselected`.
- `.venv/bin/pytest apps/backend/tests/test_agent_sessions.py -q -k "prepare_session_workspace_exposes_backend_python_runtime"`: `1 passed, 123 deselected`.
- `.venv/bin/pytest apps/backend/tests/test_agent_sessions.py -q -k "research_findings_request_rejects_out_of_range_source_index or turn_prompt_keeps_chat_update_human_facing_not_internal_changelog"`: `2 passed, 122 deselected`.
- `.venv/bin/pytest apps/backend/tests/test_agent_sessions.py -q -k "notebook_file_request or model_diagnostics_notebook_request_requires_diagnostic_manifest"`: `10 passed, 114 deselected`.
- `.venv/bin/pytest apps/backend/tests/test_agent_sessions.py -q -k "pilot_outcome_scoring_worker"`: `1 passed, 123 deselected`.
- `.venv/bin/pytest apps/backend/tests/test_api_flow.py -q -k "pilot_phase_vertical"`: `1 passed, 123 deselected, 1 warning`.
- `.venv/bin/pytest apps/backend/tests/test_agent_sessions.py apps/backend/tests/test_api_flow.py -q -k "pilot_outcome_scoring_worker or pilot_phase_vertical or pilot_validation_audit"`: `4 passed, 244 deselected, 1 warning`.
- `.venv/bin/python -m py_compile apps/backend/tabular_harness/services/agent_session_results.py apps/backend/tabular_harness/services/agent_sessions.py`: passed.
- `.venv/bin/python -m py_compile apps/backend/tabular_harness/services/agent_sessions.py apps/backend/tabular_harness/worker/jobs.py`: passed.
- `.venv/bin/python -m py_compile apps/backend/tabular_harness/services/model_diagnostics_artifacts.py apps/backend/tabular_harness/worker/jobs.py apps/backend/tests/test_model_diagnostics_artifacts.py apps/backend/tests/test_api_flow.py`: passed.
- `.venv/bin/python -m py_compile apps/backend/tabular_harness/services/research_plans.py apps/backend/tabular_harness/services/agent_sessions.py apps/backend/tests/test_research_plan_timeline.py apps/backend/tests/test_agent_sessions.py`: passed.
- `.venv/bin/python -m py_compile apps/backend/tabular_harness/services/agent_sessions.py apps/backend/tests/test_agent_sessions.py`: passed.
- `.venv/bin/python -m py_compile apps/backend/tabular_harness/worker/jobs.py apps/backend/tests/test_agent_sessions.py apps/backend/tests/test_api_flow.py`: passed.
- `.venv/bin/pytest apps/backend/tests/test_agent_sessions.py -q -k "prepare_session_workspace or duplicate_public_marimo_variables or runtime_broken_marimo_source"`: `3 passed, 119 deselected`.
- `.venv/bin/pytest apps/backend/tests/test_agent_sessions.py -q -k "notebook_file_request or prepare_session_workspace"`: `10 passed, 112 deselected`.
- `.venv/bin/python -m pytest apps/backend/tests/test_analysis_notebooks.py -q -k "duplicate_public_cell_variables"`: `1 passed, 24 deselected`.
- `git diff --check`: passed again after the latest 0119 fixes.
- `git diff --check`: passed after adding initial ResearchPlan deliverable contracts for data understanding and prior research.
- `curl http://127.0.0.1:5173/api/auth/status`: HTTP `200`.
- `codex --version`: `codex-cli 0.142.2`.
- Temporary Codex workspace network check with `sandbox_workspace_write.network_access=true` and `web_search="live"`: succeeded with `HTTP/2 200` from `https://example.com` and no deprecated `--enable web_search` event.
- Independent current-code API smoke at `http://127.0.0.1:8019` with isolated data dir `/tmp/tablex-0119-e2e-data-2`: upload -> Full Auto start -> Raw stdout growth -> ResearchPlan `current_work` accepted -> Chat progress -> stop succeeded. Evidence: `docs/evidence/0119_live_full_auto_api_smoke_after_plan_request_fix.json`.
- Independent current-code API smoke after replacing workspace Python symlinks with exec wrappers at isolated data dir `/tmp/tablex-0119-e2e-data-3`: upload -> Full Auto start -> Raw stdout growth -> stop succeeded. Evidence: `docs/evidence/0119_live_full_auto_api_smoke_after_python_wrapper.json`.
- Manual runtime check inside the `/tmp/tablex-0119-e2e-data-3` AgentSession workspace with `.tablex/bin` first in `PATH`: `python` resolved to `/home/yuya/Tablex/.venv/bin/python` and could import pandas, numpy, sklearn, matplotlib, plotly, and marimo.
- Longer isolated Full Auto salary smoke at isolated data dir `/tmp/tablex-0119-e2e-long`: upload -> Full Auto start -> Codex generated EDA/modeling artifacts -> ResearchPlan current_work moved through `data_understanding`, `evaluation_modeling`, and `notebook_reporting` -> 24 leaderboard rows registered -> 2 native notebook sources registered -> stop succeeded. Evidence: `docs/evidence/0119_live_full_auto_long_salary.json`. This is not a pass for the full product scenario because pipeline bundles were not registered, external source-backed research was not produced, and the notebook quality manifest repair requests initially failed before the latest compatibility fix.
- Isolated Full Auto pipeline smoke at isolated data dir `/tmp/tablex-0119-e2e-pipeline`: upload -> Full Auto start -> Leaderboard rows registered -> pipeline registration inbox generated -> Codex submitted pipeline directories and requests. Evidence: `docs/evidence/0119_live_full_auto_pipeline_smoke.json`. The original run did not complete Workstream C because all 42 pipeline ACKs failed on request/manifest shape issues; this exposed the need for pipeline request compatibility and batched manifest issues.
- Re-validation against that same Codex-generated workspace after the pipeline compatibility fix: `req_0039_register_model_ridge_public_features` now registers a `prediction_pipeline` artifact (`art_306e4da4a273`) and passes smoke validation using `manifest.source_data_workspace_path` as the smoke input source. Compatibility warnings are recorded for fixed-format manifest normalization instead of silently changing the model logic.
- Re-validation against the later isolated Codex-generated workspace `/tmp/tablex-0119-e2e-pipeline-rerun` showed the next remaining pipeline failure is not schema shape but an incomplete bundle: `predict.py` tried to load `model.joblib` that was not included in the pipeline directory. Current code returns this as a structured `PipelineToolValidationError` with `issues[0].pointer == "pipeline.predict"` and a repair hint, so Codex can fix the bundle without Tablex inferring or supplying model files.
- Short isolated Full Auto final-attempt smoke at isolated data dir `/tmp/tablex-0119-e2e-pipeline-final`: upload -> Full Auto start -> Raw stdout growth to 95 lines -> ResearchPlan invalid commits rejected with structured errors -> Codex preserved completed nodes and set `data_understanding` as live current work -> static HTML outputs rejected as non-notebook artifacts -> stop succeeded. Evidence: `docs/evidence/0119_live_full_auto_pipeline_final_attempt.json`. This was intentionally time-bounded and stopped while Codex was still running model comparison, so it did not reach Leaderboard or Pipeline registration.
- Longer isolated Full Auto smoke at isolated data dir `/tmp/tablex-0119-e2e-long-pipeline`: upload -> Full Auto start -> Codex generated notebook, leaderboard, and pipeline requests -> 18 experiment runs -> 18 `prediction_pipeline` artifacts -> 4 `analysis_notebook` artifacts -> 37 agent chat turns -> 707 transcript events -> stop succeeded. Evidence: `docs/evidence/0119_live_full_auto_long_pipeline_after_attention_dedupe_manual_summary.json`. This run predated the latest pipeline alias compatibility patch; the 126 failed pipeline ACKs in the evidence are the fixed-format alias friction that the latest tests now cover, while 18 later pipeline requests succeeded in the same smoke.
- Current isolated Full Auto smoke at isolated data dir `/tmp/tablex-0119-current-e2e`: upload -> Full Auto start -> Codex performed quantified EDA/model comparison work -> 2 native notebook sources registered -> 12 Agent Chat turns -> Raw stdout reached 113 lines -> ResearchPlan current_work moved from `data_understanding` to `prior_knowledge_research` -> stop succeeded at the 12 minute smoke boundary. Evidence: `docs/evidence/0119_live_full_auto_current_smoke_resume_20260706T063546Z.json`. This was not a full product pass because Leaderboard rows, prediction pipelines, and source-backed research findings had not yet been registered before the time boundary. It also reproduced a UX gap: the main turn can be active for several minutes before Codex-authored chat updates arrive.
- Current 30-minute isolated Full Auto smoke at isolated data dir `/tmp/tablex-0119-current-long`: upload -> Full Auto start -> Codex produced EDA, notebook requests, model results, leaderboard rows, and prediction pipeline requests -> final snapshot had 2 notebooks, 7 leaderboard rows, 7 prediction pipelines, 30 chat turns, 389 stdout transcript lines, and current work `evaluation_modeling` -> stop succeeded at timeout. Evidence: `docs/evidence/0119_live_full_auto_current_long_20260706T065350Z.json`. This is still not a full pass because registered prior-research findings and pilot reports remained at 0.
- Process-leak check after the final full backend run found no remaining `codex exec --ignore-user-config` process from the test suite or live smokes.
- Process-leak check after the longer isolated Full Auto smoke found no remaining `8019`, `p_1de5cbf8666d`, `ags_5c5b19bb56ac`, or `codex exec --ignore-user-config` process from that smoke.
- `git diff --check`: passed after the latest pipeline compatibility changes.
- Playwright CLI with Firefox opened the running app at `http://localhost:5173`, navigated to project views, and captured `docs/evidence/playwright/0119_project_default_tabs.png`, `docs/evidence/playwright/0119_leaderboard_humanized.png`, and `docs/evidence/playwright/0119_notebook_metadata_and_runtime_attention.png`.
- Playwright CLI with Firefox opened an isolated seeded 0119 project at `http://127.0.0.1:5174/#/projects/p_d4c558cad15c` and captured `docs/evidence/playwright/0119_pipeline_bundle_and_pilot_leaderboard.png`, `docs/evidence/playwright/0119_pilot_scoring_markdown_preview.png`, and `docs/evidence/playwright/0119_validation_audit_markdown_preview.png`.

## Workstream Evidence

### A. Full Auto Stall Visibility

Evidence present:

- Config defaults exist for `agent_idle_timeout_seconds=15*60` and `agent_turn_start_silence_timeout_seconds=5*60`.
- Tests cover turn-start silence recovery and attention chat deduplication in `apps/backend/tests/test_agent_sessions.py`.
- API tests now verify a `turn_start_silence` attention event appears both in Agent Chat history and as a recovering Agent Activity worker with the source chat artifact linked.
- Progress nudges now register a deduped factual Agent Chat notice when a running main session has gone stale without a human-facing Codex progress update. This does not summarize or interpret the work; it records that execution is still active and Codex has been asked to post a short status update. The behavior is covered by `test_supervisor_safe_progress_update_uses_project_locale_without_browser_polling`.

Still open:

- A live browser/session smoke proving the user-facing Activity/Chat behavior under an actual Codex process timeout has not been run in this checkpoint.

### B. Prior Knowledge Research

Evidence present:

- Settings and runner args support session network/web-search flags.
- AgentSession context now includes explicit `agent_capabilities.network_access_enabled`, `agent_capabilities.web_search_enabled`, and the research instruction that Codex should register source-backed findings or an explicit `no_findings` decision after searching. The same context exposes `research_tool_requests.completion_contract`, so the contract is passed through the workspace rather than implied by UI state.
- `CodexCliRunner` now derives Codex network/web-search config from `ExecutionPolicy`; `network="full"` passes `sandbox_workspace_write.network_access=true` and `web_search="live"` instead of the deprecated `--enable web_search`.
- `tablex_research_request.v1` style request handling is covered by tests for findings, schema error, and `no_findings`.
- Successful `register_research_findings` requests now register a harness-factual Agent Chat turn that links directly to the `research_findings_report` artifact. The message uses only fixed-format payload fields such as topic/source count/finding count/no_findings and does not synthesize or judge the research claims.
- A registered `research_findings_report` artifact now satisfies ResearchPlan deliverables declared as `research_findings`, `research_findings_report`, `prior_research`, `evidence`, or `report`. This lets Codex mark a prior-research chapter done using the registered finding artifact as completion evidence without Tablex reading or grading the prose.
- Agent Activity can now use the research registration chat turn as its latest focus, and ordinary Chat update action linking can include the latest `research_findings_report` artifact alongside notebooks and leaderboard results.
- `research_findings_report` JSON artifacts now preview as a Markdown source/finding list with source URLs and source indexes rendered from the registered payload. This is a structural rendering of Codex's submitted data, not a harness-authored synthesis of the claims.
- Home's Ideas & Findings panel now includes recent `research_findings_report` artifacts as openable finding signals. The visible title/summary come from artifact metadata fields such as topic, source_count, finding_count, and no_findings; clicking the signal opens the artifact preview instead of relying on a separate Assets search.
- Docs include the local command to inspect Codex harness network args.
- A live temporary Codex session workspace network check completed successfully with `curl -I --max-time 20 https://example.com`.

Still open:

- A project-level Full Auto prior-knowledge research run has not yet been manually verified to produce source-backed findings in the UI.

### C. Prediction Pipeline

Evidence present:

- Pipeline request/ack, bundle registration, validation smoke paths, and prediction worker paths are covered in backend tests.
- Pipeline request handling now accepts fixed-format compatibility shapes observed from live Codex sessions: top-level pipeline payload fields when `payload` is omitted, `run_ids` as an alias for `experiment_run_ids`, explicit `manifest_workspace_path`, and manifest contracts expressed as `input_contract.required_columns` / `output_contract.required_columns`. ACKs and artifact metadata record `compatibility_warnings` and preserve the submitted manifest when it was normalized.
- Pipeline manifest validation now returns batched structured `error.issues` in ACKs instead of forcing Codex through one-field-at-a-time repair loops.
- Pipeline smoke/runtime failures now also return structured `error.issues` in ACKs. A missing model artifact during `predict.py` execution is surfaced as `pipeline.predict` with stderr and a repair hint, rather than as an unstructured failure or a harness-side attempt to fill in the model file.
- Pipeline smoke validation now uses a row from `manifest.source_data_workspace_path` when present, including `.tablex/data` symlinked DatasetSnapshot paths. Synthetic contract values remain only for pipelines that do not declare a source data path.
- Pipeline registration smoke validation now runs `predict.py` with a cached isolated venv under `data/_pipeline_envs/<python-tag>_<requirements-hash>`, using `requirements.txt` as the environment contract. The smoke metadata records `runtime_isolated=true` and the requirements hash.
- Pipeline registration requests now ACK as `queued` during AgentSession workspace ingestion and run the smoke validation, bundle registration, final ACK, transcript event, and lineage updates in the `register_prediction_pipeline` worker job. The AgentSession ingestion path no longer runs the heavy pipeline smoke synchronously.
- Prediction jobs now execute `predict.py` through the same cached isolated venv contract when a registered bundle contains `requirements.txt`; prediction batch metadata records `runtime_isolated`, `python_executable`, and `requirements_hash`.
- Backend tests include a time-series-style prediction pipeline worker fixture that passes a separate `history_artifact_id` into `predict.py`, where the pipeline recomputes a lag-style rolling value internally and registers the resulting prediction batch without Tablex computing the derived feature.
- Leaderboard bundle download API is covered in backend API tests.
- Browser evidence in `docs/evidence/playwright/0119_pipeline_bundle_and_pilot_leaderboard.png` shows a leaderboard row with human-facing model name, description, features, score, notebook evidence, and a `Download reproducible pipeline` link backed by `GET /api/experiment-runs/{run_id}/pipeline-bundle`.

Still open:

- Product-level UI flow from a newly generated Codex-authored model to downloadable pipeline bundle has direct backend/live-workspace evidence for one Codex-generated pipeline, but a fresh browser-visible Full Auto walkthrough after the compatibility fix has not yet been completed.

### D. Pilot Phase

Evidence present:

- Pilot deployment entities, prediction batch, outcome scoring, `pilot_scoring_report`, and validation-audit request handling are present and covered by backend tests.
- A vertical API/worker test now covers: Codex-submitted `register_prediction_pipeline` request -> worker-smoked pipeline bundle registration -> pipeline bundle download -> pilot deployment -> two prediction batches through the queued prediction worker -> outcome ingestion -> queued pilot scoring worker -> `pilot_scoring_report` artifact -> main AgentSession inbox notification, Raw transcript event, and Agent Activity focus on the pilot report.
- The same vertical test now verifies that a registered `validation_scheme_audit` artifact is returned through the Pilot deployment index, so the Leaderboard pilot section can open the audit alongside scoring reports instead of burying the validation-scheme review in the asset library.
- `pilot_scoring_report` and `validation_scheme_audit` artifacts now satisfy ResearchPlan deliverables such as `pilot_scoring`, `pilot_report`, `validation_audit`, and `pilot_audit`. This connects Pilot observation and validation audit outputs to the same plan completion contract as notebooks, reports, and leaderboard runs.
- Pilot observation delivery now targets active Full Auto session states including `between_turns` and `waiting_for_runner`, not only a currently-running Codex process. The worker test verifies a `between_turns` main session still receives the inbox notice.
- Pilot outcome scoring now queues the existing Full Auto continuation heartbeat after writing the `pilot_scoring_report` and delivering the observation to the main session inbox. This keeps the pilot observation on the continuing main Codex path instead of relying only on periodic supervisor recovery or creating a separate interpretation job.
- ResearchPlan request ingestion now processes `commit_revision` before `set_current_work` when multiple structured plan requests are submitted in the same workspace scan. This removes a file-name-order footgun where Codex could correctly submit a follow-up plan and current-work declaration together, but `set_current_work` would be evaluated against the old plan first.
- A backend test now covers the fixed-format pilot follow-up loop after scoring: register `validation_scheme_audit`, commit a ResearchPlan revision that marks the pilot review done with `pilot_scoring` and `validation_audit` evidence, and set the next iteration as current work in the same main session workspace.
- ResearchPlan timeline artifact/run link lookup now dedupes ids and queries them in bounded chunks. This fixes a real SQLite `too many SQL variables` failure observed on an existing project with many duplicate plan-support edges, without changing Codex-authored plan text or status.
- `agent_interface_spec.md` contains a Pilot Phase section.
- Browser evidence in `docs/evidence/playwright/0119_pipeline_bundle_and_pilot_leaderboard.png` shows the Pilot deployments section next to the leaderboard with prediction/outcome batch counts, latest score, as-of checks, and validation audit summary.
- Browser evidence in `docs/evidence/playwright/0119_pilot_scoring_markdown_preview.png` and `docs/evidence/playwright/0119_validation_audit_markdown_preview.png` shows pilot scoring and validation audit artifacts opening as structural Markdown previews rather than raw JSON. The preview is fixed-format rendering of registered payload fields and hides internal deployment/request identifiers from the normal UI.

Still open:

- A manual UI walkthrough of pilot deployment and report/audit visibility has been run against a seeded isolated fixture. Outcome upload from the browser and a live Codex follow-up iteration that consumes the pilot observation have not been run in this checkpoint.
- Automated tests now cover the structured follow-up outputs that a main Codex turn should submit after consuming a pilot observation, but a live Codex follow-up turn performing that behavior in-product has not been run in this checkpoint.

### E. Leaderboard Humanization

Evidence present:

- Leaderboard API returns `summary_md`, `model_description`, `features_used`, and `pipeline_artifact_id`.
- `register_runs` requires `model_description`/`features_used` in new payloads and returns structured ack errors for invalid submissions.
- The frontend `LeaderboardTab` is separated and displays model label/description/feature summary rather than making internal IDs primary.
- The frontend distinguishes a declared live ResearchPlan current_work node from the plan's active position. If Codex is running but has not declared current_work, the plan presence text says that directly instead of visually implying Codex is working inside the active plan block.
- Mission Control now uses the same distinction: when Codex is running but no validated `current_work` has been declared, the hero says the plan position is undeclared instead of presenting the active plan block as the execution location.
- The AgentSession supervisor support path now writes `.tablex/inbox/research_plan_current_work_request.md` and a `research_plan_current_work_requested` transcript event when a live session has no stored `current_work` declaration for the latest ResearchPlan revision. This asks Codex to submit `set_current_work` or revise the plan first, without Tablex guessing the node or blocking the work.
- The same support path now also asks for a refreshed `set_current_work` when Codex output has advanced after the last current-work declaration. This is timestamp/process-state based only; Tablex does not infer the node from command text or natural language.
- ResearchPlan request handling now accepts two explicit fixed-format compatibility forms observed from live Codex sessions: `research_plan_path` for `commit_revision` and `research_plan_node_id` as an alias for `node_id`. These are not natural-language fallbacks; both are schema-level fields and return `compatibility_warnings` in the ACK so future Codex requests can converge on the preferred payload shape.
- ResearchPlan request failure attention is deduped by operation/error/issue signature rather than request id. The same structural error no longer fills Chat with repeated `needs attention` cards when Codex retries with a new request id.
- The artifact ingest path and explicit `.tablex/requests/research_plan` path now use the same structural ResearchPlan failure attention key. Repeated commit failures with the same schema/ordering issue produce one Chat attention card instead of one card per request artifact.
- Live smoke after this change showed ResearchPlan `set_current_work` accepted with a compatibility warning and the visible current work became `data_understanding` without repeated ResearchPlan failure chat entries.
- The longer isolated salary smoke registered 24 leaderboard rows with `model_description` and `features_used` visible through the API. The leading row was `ridge_onehot` with `mae=9370.685108998709`. Pipeline artifacts were still absent for those rows, so Workstream C/E remains incomplete at product level.
- The longer isolated pipeline smoke registered 18 leaderboard rows and 18 `prediction_pipeline` artifacts in one Full Auto session. This proves the main-session path can reach pipeline registration, but it does not yet replace a browser walkthrough because the smoke was stopped while Codex was still repairing a final marimo variable-name issue.
- Experiment result ACKs now include a fixed `pipeline_registration` status. When registered Leaderboard runs do not yet have linked `prediction_pipeline` artifacts, Tablex writes `.tablex/inbox/pipeline_registration_request.md` with the missing run ids and the `register_prediction_pipeline` contract, and the user-facing Chat summary states that reproducible train/predict scripts are still pending. This does not generate pipelines or infer model logic; it returns the structured omission to Codex so the same main session can continue.
- Experiment result ACKs now also include a fixed `model_diagnostics_notebook` status. When registered Leaderboard runs do not yet have linked model-diagnostics marimo notebooks, Tablex writes `.tablex/inbox/model_diagnostics_notebook_request.md` with the missing run ids and the `register_notebook` contract, and the Agent Chat registration summary states that interpretation notebooks are still pending. The request points Codex to the standard diagnostic coverage fields for permutation importance, native tree feature importance, partial dependence, and SHAP without generating the analysis in harness code.
- Experiment result registration now rejects an explicit `research_plan_node_id` when that node exists but is still `pending`, unless Codex has first declared it as current work or committed a revision that makes it active/done. This is a fixed-format tool boundary: Tablex does not infer the correct node, and the structured ACK tells Codex to repair the plan/current-work declaration.
- Agent Chat live glow now applies only when the bottom/latest conversation turn is active; older pending-looking turns no longer keep a moving highlight elsewhere in the history.
- Browser evidence in `docs/evidence/playwright/0119_leaderboard_humanized.png` shows the default leaderboard readout centered on model names, descriptions, scores, and evidence links rather than run ids.
- Browser evidence in `docs/evidence/playwright/0119_pipeline_bundle_and_pilot_leaderboard.png` shows the pipeline bundle link and Pilot deployments readout in the same leaderboard view. `docs/evidence/playwright/0119_pilot_scoring_markdown_preview.png` and `docs/evidence/playwright/0119_validation_audit_markdown_preview.png` verify linked pilot artifacts open without raw JSON or maker-facing identifiers in the normal preview.
- Seeded browser evidence in `docs/evidence/playwright/0119_seeded_full_slice_home_updated.png` shows ResearchPlan blocks for data upload, data understanding, prior research, modeling/pipeline, pilot review, and next iteration with a paused current work declaration rather than process-presence inference.
- Seeded browser evidence in `docs/evidence/playwright/0119_seeded_full_slice_leaderboard_pilot.png` shows the same product-slice project exposing the human-facing model row, reproducible pipeline link, and Pilot deployment/scoring/audit section.

Still open:

- Product-level UI flow from a newly generated Codex-authored model to a registered downloadable pipeline bundle remains covered by tests but not manually walked through in this checkpoint.

### F. Notebook Data Access And Native Marimo

Evidence present:

- Session workspace data access exposes `.tablex/data` paths in context.
- Session workspace Python shims are now executable wrappers to the backend venv Python rather than symlinks. A symlinked Python executable made `python` run but lost venv package discovery; the wrapper preserves the real executable path so Codex workspace commands can import installed analysis/runtime packages.
- Backend tests cover `.tablex/data` workspace preparation and native marimo runtime failure indexing.
- A native marimo runtime test now opens a notebook whose source reads `.tablex/data/ds_native_data_access__train.csv` from the session workspace, builds Plotly figures and a table from that CSV, and reaches `running` without a runtime error. This verifies the data-access contract through the native marimo path rather than through static HTML fallback.
- Static HTML is not treated as the regular notebook fallback in the 0119 directive.
- Native marimo iframe titles and expanded toolbar badges no longer display notebook artifact ids in the normal UI.
- Browser evidence in `docs/evidence/playwright/0119_notebook_metadata_and_runtime_attention.png` shows the Notebooks tab rendering the registered Notebook index, declared figure count, read order, key findings, limitations, and a native marimo runtime error as an explicit repair target instead of hiding it or labeling it as readable evidence.
- Notebook normal UI labels now show source category such as dataset, model, or experiment result without appending run/model/dataset internal ids. The ids remain available to the linking logic and artifact details, not as the primary human-facing label.
- Notebook registration now runs a transient native marimo runtime preflight via `marimo export html` into a temporary file. The HTML output is not registered or shown as a fallback; failures become structured notebook request ack errors so Codex can repair and resubmit.
- Native marimo runtime failures are now recorded with the current Notebook source hash. The Notebook index only applies a runtime-error state when the failure hash matches the current source, so an old failure does not keep marking a repaired source as broken.
- Native marimo session startup no longer waits for the notebook server to become ready before returning to the UI. The session payload reports `starting`, `running`, or `failed`; the frontend shows a starting state and polls instead of loading the iframe through the proxy before marimo is ready. A failed startup remains observable through the session status endpoint and is registered as a notebook repair target in Chat/Activity.
- A real native marimo process test now opens a figure-rich `analysis_notebook` artifact, waits for the session to become `running`, fetches the proxied HTML, and then stops the session. The fixture notebook includes three Plotly figures and a table; this is a runtime/openability proof, not a harness-authored notebook template.
- The Notebook index no longer hides an invalid native marimo source. Static HTML still stays out of Notebook evidence, but a Python source that imports/defines marimo and fails fixed source validation is listed with `native_marimo_status=source_error` and `status=needs_attention`, so the user can see the repair target instead of seeing an empty notebook surface.
- Seeded browser evidence in `docs/evidence/playwright/0119_seeded_full_slice_notebooks_narrative_ready.png` shows the product Notebooks tab with 1 native source, 2 declared figures, read order, key finding, normal notebook actions, and `narrative ready` display sourced from the fixed quality manifest.
- Seeded browser evidence in `docs/evidence/playwright/0119_seeded_full_slice_native_marimo_running.png` shows the same notebook opened through the native marimo iframe with session state `running`.
- Notebook request handling now accepts two explicit fixed-format compatibility forms observed in the long live salary smoke: notebook registration fields at the request top level when `payload` is omitted, and `quality_manifest.read_order` as a string list that is preserved as label-only read-order entries. ACKs include `compatibility_warnings`; this does not generate or interpret notebook prose.
- Notebook request failure attention is now deduped by operation/error signature rather than request id. Repeated structural failures no longer fill Chat with duplicate `needs attention` cards while Codex repairs the same fixed-format request shape.
- Notebook tool context now tells Codex that marimo public variables must be unique across cells and that repeated temporaries should be private underscore-prefixed names. This is authoring guidance for Codex, not a backend notebook template.
- Notebook registration now rejects duplicate public marimo cell variables through source validation before running the native marimo preflight. The ACK surfaces the fixed graph violation directly, so Codex can repair the source without a long runtime traceback.
- Notebook request failure ACKs now include structured `error.issues[]` with `pointer`, `code`, and `fix` for fixed-format notebook source/quality/runtime validation errors. The workspace inbox rejection also lists Top issues, matching the ResearchPlan tool pattern instead of forcing Codex to parse a prose-only error.
- Registration-time native marimo preflight now keeps both the head and tail of long stderr output, preserving the actual exception cause in repair ACKs. When preflight cannot start because of a runtime environment boundary such as sandboxed socket creation or missing marimo, the source registration is not blocked by that environment-only check; native marimo open/runtime failures remain surfaced through the notebook runtime path.
- The `tablex-grandmaster-eda` marimo output guidance now includes native marimo graph constraints, `.tablex/data`/cached-summary guidance for fast opening, and Japanese font guidance that prefers reliable viewer text or `japanize_matplotlib` only when available.
- Model-diagnostics notebook registration now requires a fixed `quality_manifest.model_diagnostics` coverage manifest for `permutation_importance`, `native_feature_importance`, `partial_dependence`, and `shap`. This is schema validation only: Codex owns the analysis and may mark a check included, not applicable, dependency-bound, model-artifact-bound, or deferred with evidence/reason. Backend tests cover both the rejection path and successful registration.
- Leaderboard run registration now writes a model-diagnostics notebook request to the AgentSession inbox when the run is not linked to a model-diagnostics marimo notebook. This keeps the diagnostic notebook as a Codex-authored deliverable and avoids harness-side interpretation while reducing silent omission of standard model evidence.
- Model-diagnostics artifact materialization now emits fixed data slots for bounded partial dependence and SHAP summary in addition to native feature importance and permutation importance. PDP is computed from the stored model package and validation split when available, using supported native tree feature-importance APIs first (`feature_importances_`, `get_feature_importance`, booster score), then permutation-importance ranking when native importance is unavailable. SHAP is stored as ready or a reasoned blocked state depending on runtime/model support. The worker output returns `partial_dependence_artifact_id` and `shap_summary_artifact_id`, and the visualization spec includes corresponding data views without generating notebook narrative.
- ResearchPlan output contracts now expose fixed known output types for source-backed research, prediction pipelines, model diagnostics, native/permutation importance, PDP, SHAP, pilot scoring, and validation audits. Registered artifacts with matching asset types satisfy those deliverables through fixed asset-type mapping rather than title/text inference.
- Open ResearchPlan nodes with `deliverable_contract.expected_outputs` are now preserved by fixed-format validation across revisions. Codex may rename, split, or replace the node, but the new revision must keep the same expected output contract until it is completed, skipped with structured rationale, or satisfied by registered evidence. This prevents prior-knowledge research or other required deliverables from vanishing while keeping plan authorship with Codex.
- The harness-owned initial ResearchPlan anchors now declare fixed deliverable contracts for the first Codex-owned evidence boundaries: `data_understanding` requires a registered native notebook deliverable, and `prior_knowledge_research` requires registered source-backed research findings. This keeps Codex free to decide the analysis while preventing these chapters from being marked complete by process presence or title text alone.
- ResearchPlan artifact output typing now only lets `analysis_notebook`/notebook-like artifacts satisfy a `notebook` deliverable when the registered primary source is native marimo Python. Static HTML snapshots can still be stored or rejected as artifacts, but they no longer complete notebook plan evidence.
- Failed `register_research_findings` requests now write `.tablex/inbox/research_request_rejection.md` in addition to the ack JSON, and the main turn prompt lists it as a long-turn repair target. This gives Codex the same fixed-format self-repair path for prior-research submissions that ResearchPlan, Notebook, Leaderboard, and Pipeline requests already use.
- AgentSession context and turn prompts now also include `.tablex/inbox/pilot_observation_available_*.md` and the `.tablex/requests/pilot` `register_validation_audit` contract. This means Pilot scoring reports are handed back to the main Codex session as fixed-format observations for audit and next-iteration planning instead of remaining only as side artifacts.
- Pilot scoring notification now copies the `pilot_scoring_report` JSON into the main AgentSession workspace under `.tablex/pilot_observations/<artifact_id>.json` and includes that relative path in the inbox notice. This removes a workspace-readability gap for long-running Codex turns: the notice contains both the summary metrics and a stable workspace path to the full fixed-format report.
- Agent Chat notebook update grouping now keeps the latest action for a notebook/update group. An old `needs_attention` action for the same notebook is no longer shown beside a later `ready`/`source_saved` action after Codex repairs and re-registers the notebook.
- The preflight was manually run against the currently broken live artifact `art_d6c8be7af5b0` and detected the runtime error: `MarimoExceptionRaisedError: 'best_oof_abs_error'`.
- A text audit for `SK_ID_CURR`, `Home Credit`, `application_train`, and related identifiers found them in benchmark fixtures, docs, tests, and curated benchmark/asset-library references, not in product notebook-authoring templates. This does not prove every future Codex-authored notebook is correct; it confirms the harness is not currently shipping a fixed Home Credit/SK_ID_CURR notebook template.
- A text audit for static notebook HTML found product paths that reject or hide `notebook_html`/`notebook_execution_html` snapshots and tests/docs enforcing native marimo source as the artifact of record. Static HTML remains only as legacy/rejection coverage and transient preflight implementation detail.

Still open:

- A successful product-level native marimo walkthrough now exists for the seeded product-slice fixture. A live Full Auto walkthrough with a newly Codex-authored project notebook remains open.
- The short live smokes intentionally stopped before Codex produced a registered Notebook or Leaderboard result. They verify motion, Raw/Chat visibility, plan current-work alignment, and workspace runtime availability, not the full data-science deliverable loop.
- The longer live salary smoke proved that Codex can create notebooks and leaderboard rows in one session, but the notebook quality repair loop was not re-run after the latest request compatibility change. A successful product-level native marimo notebook walkthrough remains open.
- The 30-minute isolated Full Auto smoke in `docs/evidence/0119_live_full_auto_current_long_20260706T065350Z.json` reached 2 notebooks, 7 leaderboard rows, 7 prediction pipelines, 30 chat turns, 389 stdout transcript lines, and current work `evaluation_modeling` before the test driver stopped it at timeout. It did not reach registered prior-research findings or pilot reports, so Workstreams B and D remain open at product-walkthrough level.

### G. UI Pruning

Evidence present:

- `main.tsx` is now an entrypoint.
- `ResearchPlanTimeline`, `AgentChatDock`, `RawAgentStream`, `rawEvents`, `AgentActivityRail`, `ArtifactPreview`, `LeaderboardTab`, and `NotebookLinks` are independent frontend modules.
- Shared display primitives and the workbench error boundary are now independent frontend modules, keeping `App.tsx` below the 12,000-line target with more margin.
- Default top-level tab set is constrained to Home, Data, Insight, Leaderboard, and Assets, with supporting tabs grouped under More.
- Raw is no longer a top-level or More-menu tab. It remains available only as the detailed execution-record view inside Home's Agent workspace Chat/Raw switch.
- Agent Activity cards show project, work type, and elapsed time without surfacing job/session ids in the normal card body.
- Browser evidence in `docs/evidence/playwright/0119_project_top_nav_home_data_insight_leaderboard_assets.png` verifies the restored project navigation: Home, Data, Insight, Leaderboard, Assets, and More. Raw is no longer a top-level or More-menu tab; it remains available only as the Agent workspace Chat/Raw detail toggle on Home.

Still open:

- `App.tsx` is now below the 12,000-line target, but should continue to be split as the UI stabilizes.
- Further UI pruning should continue after the end-to-end Full Auto validation stabilizes; Raw's default navigation placement is now browser-verified.

## Current Frontend File Sizes

- `apps/frontend/src/main.tsx`: 12 lines.
- `apps/frontend/src/App.tsx`: 11985 lines.
- `apps/frontend/src/components/rawEvents.ts`: 382 lines.
- `apps/frontend/src/components/ArtifactPreview.tsx`: 453 lines.
- `apps/frontend/src/components/AgentActivityRail.tsx`: 844 lines.
- `apps/frontend/src/components/RawAgentStream.tsx`: 411 lines.
- `apps/frontend/src/components/AgentChatDock.tsx`: 691 lines.
- `apps/frontend/src/components/LeaderboardTab.tsx`: 823 lines.
- `apps/frontend/src/components/NotebookLinks.tsx`: 187 lines.
- `apps/frontend/src/components/ResearchPlanTimeline.tsx`: 334 lines.
- `apps/frontend/src/components/WorkbenchErrorBoundary.tsx`: 30 lines.
- `apps/frontend/src/components/Primitives.tsx`: 85 lines.

## 2026-07-06 Addendum

- User-requested model diagnostics coverage is now represented as fixed diagnostic artifact slots and tests: permutation importance, native/tree feature importance (`feature_importances_`, `get_feature_importance`, and booster gain score), partial dependence for top-ranked supported features, and SHAP summary when runtime/model support is available. This is artifact materialization and schema-backed coverage, not harness-authored model interpretation.
- Model-diagnostics notebook registration now supports `related_run_ids`, so one Codex-authored native marimo notebook can formally cover multiple leaderboard rows. The notebook index, leaderboard evidence lookup, Chat action surfaces, and registration ACK all preserve those run links instead of relying on filename or text inference.
- `uv run pytest apps/backend/tests/test_agent_sessions.py::test_experiment_result_file_request_registers_leaderboard_run_with_ack apps/backend/tests/test_agent_sessions.py::test_model_diagnostics_notebook_request_requires_diagnostic_manifest apps/backend/tests/test_agent_sessions.py::test_notebook_file_request_links_run_model_and_dataset_context apps/backend/tests/test_agent_sessions.py::test_model_diagnostics_notebook_request_links_related_runs apps/backend/tests/test_analysis_notebooks.py::test_notebook_index_links_multiple_runs_from_same_research_plan_node -q`: `5 passed`.
- `uv run pytest apps/backend/tests/test_api_flow.py::test_leaderboard_reconciles_existing_run_into_chat_and_plan_links apps/backend/tests/test_agent_sessions.py::test_model_diagnostics_notebook_request_links_related_runs apps/backend/tests/test_analysis_notebooks.py::test_notebook_index_links_multiple_runs_from_same_research_plan_node -q`: `3 passed`.
- `uv run pytest apps/backend/tests -q`: `382 passed, 6 warnings`.
- `npm run build` in `apps/frontend`: passed; Vite still reports the existing large chunk warning.
- `uv run pytest apps/backend/tests/test_research_plan_timeline.py apps/backend/tests/test_analysis_notebooks.py apps/backend/tests/test_model_diagnostics_artifacts.py -q`: `66 passed`.
- `uv run pytest apps/backend/tests/test_research_plan_timeline.py::test_research_plan_current_work_rejects_blank_summary apps/backend/tests/test_research_plan_timeline.py::test_research_plan_timeline_ignores_legacy_blank_current_work apps/backend/tests/test_agent_sessions.py::test_research_plan_tool_rejects_blank_current_work_summary -q`: `3 passed`.
- `uv run pytest apps/backend/tests/test_analysis_notebooks.py::test_notebook_index_accepts_list_shaped_visual_story_artifact -q`: `1 passed`.
- A new isolated live Full Auto smoke was started against `/tmp/tablex-0119-after-open-contract` with project `p_7ec9c59d29e5`; the event log is `docs/evidence/0119_live_full_auto_after_open_contract_20260706.json`. The run reached Codex stdout activity, Chat updates, 36 artifacts, one native notebook index item, and ResearchPlan progression to `evaluation_and_modeling`, but did not reach leaderboard rows or pipeline artifacts before the test driver was stopped.
- That smoke exposed two fixed-format robustness issues: `set_current_work` accepted an empty `summary`, and Notebook index construction assumed a visual-story artifact was always a dict with `cards`. Both are now covered by regression tests. Future blank current-work submissions fail through the ResearchPlan ACK path, while legacy blank current-work records are ignored in timeline display in favor of the active ResearchPlan node.
- Rechecking the same isolated project after the fixes returned HTTP 200 for ResearchPlan timeline, Agent Chat history, Raw transcript, Leaderboard, Notebook index, Artifacts, and Agent Activity. The Notebook index returned one recommended native source; Leaderboard returned zero runs rather than throwing.
- Added `tablex_model_diagnostics_request.v1` / `register_model_diagnostics_artifacts` for Codex-authored model diagnostics files. The request registers permutation importance, native/tree feature importance, partial dependence, SHAP summaries or explicit unavailable statuses, and a `model_diagnostics_artifact_pack` as first-class artifacts linked to ExperimentRuns and optional ResearchPlan nodes. This is fixed-format artifact registration and link validation; it does not add harness-authored interpretation.
- `uv run pytest apps/backend/tests/test_agent_sessions.py::test_model_diagnostics_artifact_request_registers_standard_outputs apps/backend/tests/test_agent_sessions.py::test_model_diagnostics_artifact_request_rejects_missing_included_output apps/backend/tests/test_agent_sessions.py::test_model_diagnostics_notebook_request_links_related_runs apps/backend/tests/test_agent_sessions.py::test_repeated_single_run_notebook_registration_promotes_to_related_runs -q`: `4 passed`.
- `uv run pytest apps/backend/tests -q`: `385 passed, 6 warnings`.
- Isolated smoke recheck against `/tmp/tablex-0119-after-open-contract`, project `p_7ec9c59d29e5`, processed a real `register_model_diagnostics_artifacts` request from the existing AgentSession workspace. The ACK succeeded and registered:
  - `art_6956e8c5387c` as `permutation_importance`
  - `art_99c3d501a28e` as `feature_importance`
  - `art_0aee0dfdc18f` as `partial_dependence`
  - `art_c428b5da6d83` as `model_diagnostics_artifact_pack`
  These artifacts are linked to runs `run_7f6ac8aaebcf`, `run_b8e3f34dcaf1`, and `run_eeac491f6802`.
- The same isolated smoke returned HTTP 200 for ResearchPlan timeline, Leaderboard, analysis notebook index, artifacts, and Agent Activity after diagnostics registration. The `diagnostics_and_reporting` block returned `evidence_verified=True` and included attached artifacts for permutation importance, feature importance, partial dependence, and `model_diagnostics_artifact_pack`.
- Pilot validation-audit request failures now follow the same fixed-format self-repair path as ResearchPlan, Notebook, Leaderboard, Pipeline, and Model Diagnostics requests: failed `tablex_pilot_request.v1` requests write an ACK, `.tablex/inbox/pilot_request_rejection.md`, a Raw transcript sidecar event, and a deduped human-facing Chat attention turn. This exposes the failure without stopping the main Codex path or rewriting Codex-authored analysis.
- `uv run pytest apps/backend/tests/test_agent_sessions.py::test_pilot_validation_audit_request_rejects_invalid_component apps/backend/tests/test_agent_sessions.py::test_pilot_validation_audit_request_registers_artifact_evidence_and_plan_link apps/backend/tests/test_agent_sessions.py::test_pilot_observation_followup_can_commit_audited_next_iteration_plan -q`: `3 passed`.
- `uv run pytest apps/backend/tests/test_api_flow.py -q -k "pilot_phase_vertical"`: `1 passed, 123 deselected, 1 warning`.
- `uv run pytest apps/backend/tests/test_agent_sessions.py -q -k "pilot_validation_audit or pilot_observation_followup or pilot_outcome_scoring_worker or model_diagnostics_artifact_request or model_diagnostics_notebook_request"`: `8 passed, 122 deselected`.
- `uv run pytest apps/backend/tests/test_model_diagnostics_artifacts.py apps/backend/tests/test_api_flow.py -q -k "pilot_phase_vertical or partial_dependence or shap_summary or native_feature_importance"`: `8 passed, 125 deselected, 1 warning`.
- `uv run python -m py_compile apps/backend/tabular_harness/services/agent_sessions.py apps/backend/tests/test_agent_sessions.py apps/backend/tabular_harness/services/model_diagnostics_artifacts.py`: passed.
- `uv run pytest apps/backend/tests -q`: `385 passed, 6 warnings`.
- Local backend was restarted on port 8000 with the updated code. API smoke against project `p_f6b8a035271c` returned HTTP 200 for ResearchPlan timeline, Leaderboard, analysis notebooks, artifacts, Agent Activity, and Pilot deployments; frontend remains available on port 5173.
- Prior-research request failures now also follow the fixed-format self-repair path: failed `tablex_research_request.v1` submissions write an ACK, `.tablex/inbox/research_request_rejection.md`, a Raw transcript sidecar event, and a deduped human-facing Chat attention turn. This keeps source-backed research registration failures visible without having the harness infer, rewrite, or judge the research content.
- `uv run pytest apps/backend/tests/test_agent_sessions.py::test_research_findings_request_registers_report_evidence_and_plan_link apps/backend/tests/test_agent_sessions.py::test_research_findings_request_rejects_out_of_range_source_index apps/backend/tests/test_agent_sessions.py::test_research_findings_request_registers_explicit_no_findings -q`: `3 passed`.
- `uv run python -m py_compile apps/backend/tabular_harness/services/agent_sessions.py apps/backend/tests/test_agent_sessions.py`: passed.
- `uv run pytest apps/backend/tests/test_agent_sessions.py apps/backend/tests/test_research_plan_timeline.py -q -k "research_findings_request or research_request or prior_research or registered_deliverables"`: `3 passed, 156 deselected`.
- `uv run pytest apps/backend/tests/test_api_flow.py -q -k "research_findings_json_preview"`: `1 passed, 123 deselected, 1 warning`.
- `uv run pytest apps/backend/tests -q`: `385 passed, 6 warnings`.
- Local backend was restarted again on port 8000 after the prior-research attention-path update. API smoke against project `p_f6b8a035271c` returned HTTP 200 for ResearchPlan timeline, Leaderboard, analysis notebooks, artifacts, Agent Activity, and Pilot deployments; frontend remains available on port 5173.
- Experiment result registration now reports standard model-diagnostics artifact coverage separately from model-diagnostics notebooks. The ACK, Chat `response_brief`, Raw sidecar events, and `.tablex/inbox/model_diagnostics_artifact_request.md` expose missing permutation importance, native/tree feature importance, partial dependence, and SHAP coverage, then route Codex back to the existing `tablex_model_diagnostics_request.v1` / `register_model_diagnostics_artifacts` substrate. This is fixed-format artifact coverage accounting; it does not add harness-authored interpretation.
- Leaderboard rows now expose `model_diagnostics` with standard check statuses and artifact ids when available, so the table can show whether each run has permutation importance, native feature importance, PDP, and SHAP diagnostics registered or explicitly unavailable.
- `.venv/bin/pytest apps/backend/tests/test_agent_sessions.py -q -k "experiment_result_file_request_registers_leaderboard_run_with_ack or turn_prompt_keeps_chat_update_human_facing_not_internal_changelog or model_diagnostics_artifact_request_registers_standard_outputs"`: `3 passed`.
- `.venv/bin/pytest apps/backend/tests/test_api_flow.py -q -k "legacy_experiment_registration_payload or latest_codex_chat_update_links_registered_notebooks_and_leaderboard or project_upload_profile_evaluation_split_flow"`: `3 passed, 3 warnings`.
- `.venv/bin/python -m py_compile apps/backend/tabular_harness/services/agent_session_results.py apps/backend/tabular_harness/services/agent_sessions.py apps/backend/tests/test_agent_sessions.py`: passed.
- Local backend was restarted from `/home/yuya/Tablex` so it uses the main `data/metadata/app.db`. `/health` returns `{"status":"ok"}`; frontend remains on port 5173. API smoke against project `p_f6b8a035271c` returned one leaderboard row with `model_diagnostics.status="missing"` and all four standard checks surfaced as missing, which is expected for the seeded project before diagnostics artifacts are registered.
- Upload-to-objective latency was reduced by staging uploaded files immediately, storing lightweight column headers on staging/final table artifacts, and exposing those headers through `/api/projects/{project_id}/data/columns` while the heavier ingest/profile worker is still running. The target field now suggests queued CSV headers before upload, staged table columns during intake, and registered primary/supporting table columns after intake without harness-side target inference.
- The upload call now returns an initial `queued_for_ingest` progress stage at 5%, and the Data upload card keeps showing server-side ingest/profile progress from the active `upload_data_bundle` or `select_primary_table` job after file transfer reaches 100%. This keeps heavy-data import status visible in the same upload flow instead of making users infer progress from a separate small job row.
- Target/objective saving now updates the UI immediately after the Project PATCH and refreshes ResearchPlan asynchronously, so a slow timeline refresh does not block the user from continuing.
- `apps/backend/tests/test_data_upload_bundle.py` now covers staged column metadata, column catalog visibility during active intake, primary table switching, and metadata fallback with neutral fixture names instead of dataset-specific Home Credit identifiers.
- `.venv/bin/python -m py_compile apps/backend/tabular_harness/api/routes.py`: passed.
- `.venv/bin/pytest apps/backend/tests/test_data_upload_bundle.py -q`: `1 passed`.
- `npm --prefix apps/frontend run build`: passed; Vite still reports the existing large chunk warning.
- Agent Activity no longer shows availability recovery cards for the same main session alongside the main session status card. Recovery attention chat artifacts remain in Chat/Raw history, but the current Activity surface now suppresses redundant `recovering` availability workers for the active session so users do not see contradictory or duplicated "working/scheduled/recovering" states at the same time.
- `.venv/bin/python -m py_compile apps/backend/tabular_harness/api/routes.py apps/backend/tests/test_agent_activity.py apps/backend/tests/test_api_flow.py`: passed.
- `.venv/bin/pytest apps/backend/tests/test_agent_activity.py apps/backend/tests/test_data_upload_bundle.py -q`: `2 passed`.
- `npm --prefix apps/frontend run build`: passed; Vite still reports the existing large chunk warning.
- Runtime check on port 8000 showed the old backend process did not expose `/api/projects/{project_id}/data/columns`, which explained missing target column suggestions in the live UI. The old process was stopped and port 8000 was restarted with the current source via `uv run uvicorn tabular_harness.main:app --app-dir apps/backend --host 0.0.0.0 --port 8000`; `/api/health` returns `{"status":"ok"}`.
- Live `Home Credit Test` project `p_fad039e85078` was repaired from the old-upload state: `target_column` is now `TARGET`, `primary_dataset_snapshot_id` is `ds_eaaf89c5eb9c`, and the primary dataset is `application_train.csv` with 307,511 rows and 122 columns. `/api/projects/p_fad039e85078/data/columns` now returns `application_train.csv` first with `is_primary=true` and `TARGET` surfaced as a column detail with `role="target"`.
- Target column suggestions now prioritize fixed-format catalog metadata without adding harness-side target inference: columns with `role="target"` are sorted ahead of ordinary columns, and chips display registered role, primary-table status, prediction-time availability, and leakage-review flags when those fields exist in `ProjectColumnCatalog`. Queued CSV header suggestions still appear immediately before upload, but they are not interpreted as targets by the harness.
- `npm --prefix apps/frontend run build`: passed after the target suggestion metadata-badge update; Vite still reports the existing large chunk warning.
- `.venv/bin/pytest apps/backend/tests/test_data_upload_bundle.py apps/backend/tests/test_agent_activity.py -q`: `2 passed`.
- The main Full Auto turn prompt now carries the prior-research contract directly in addition to `.tablex/context.json`: if network/web search is available, Codex should use it for project-specific prior-knowledge research when it can affect target framing, validation, features, modeling, diagnostics, or reporting, then register `tablex_research_request.v1` / `register_research_findings` or explicit `no_findings` after searching. This is a prompt/contract nudge to Codex, not a harness-side content gate.
- `.venv/bin/pytest apps/backend/tests/test_agent_sessions.py -q -k "turn_prompt_includes_living_research_plan_contract or research_findings_request"`: `4 passed, 127 deselected`.
- `.venv/bin/python -m py_compile apps/backend/tabular_harness/services/agent_sessions.py apps/backend/tests/test_agent_sessions.py`: passed.
- Runtime connectivity on 2026-07-07: current UI/API are healthy on `5173/8000`; the older `5181/8021` pair is still serving a stale DB and should not be used for the current verification slice. Live project `p_3b4fd71f026a` had an active Codex process but `current_phase="UNDERSTANDING_REVIEW"`, causing Activity to report a contradictory "loop off but Codex process observed" state. The live project phase was repaired to `AUTONOMOUS_LOOP` without stopping the Codex process; `/agent-activity` now reports `Codexが作業中` and ResearchPlan current work returns `is_live=true`.
- Source fix added to prevent the same phase regression after future data upload/profile/primary-table operations: API and worker handlers now preserve `AUTONOMOUS_LOOP` instead of forcing `UNDERSTANDING_REVIEW` while Full Auto is running. `.venv/bin/python -m py_compile apps/backend/tabular_harness/api/routes.py apps/backend/tabular_harness/worker/jobs.py apps/backend/tests/test_data_upload_bundle.py`: passed. `.venv/bin/pytest apps/backend/tests/test_data_upload_bundle.py -q`: `1 passed`.
- Follow-up runtime inspection on project `p_3b4fd71f026a` showed the backend/API were correct while the Home UI could still display stale power and plan state:
  - `/api/projects/p_3b4fd71f026a` returned `current_phase="AUTONOMOUS_LOOP"`, `autonomy_mode="full_auto"`, `target_column="TARGET"`, and `primary_dataset_snapshot_id="ds_0f38bbfae59c"`.
  - `/api/projects/p_3b4fd71f026a/agent-activity` returned `state="agent_running"`, one active Codex process, and session `ags_8f559fa757ce`.
  - `/api/projects/p_3b4fd71f026a/research-plan/timeline?locale=ja-JP` returned `current_work.node_id="evaluation_modeling"`, `current_work.is_live=true`, and block `evaluation_modeling` as `active`.
  - The stale UI cause was that the workspace kept rendering from the project object originally selected from `/api/projects`, while fresh `/overview` and activity polling already had newer project state. `ProjectDetail` now synchronizes `overview.project` and the lightweight `/api/projects/{project_id}` result back into parent project state, and renders child tabs from that effective project state.
  - `ResearchPlanTimeline` now treats live `current_work` as the active/current block for display class and active-block centering, so current-work presence cannot appear only in the footer while the corresponding block still looks pending.
- Verification after the UI-state fix:
  - `npm --prefix apps/frontend run build`: passed; Vite still reports the existing large chunk warning.
  - Playwright CLI against `http://127.0.0.1:5173/#/projects/p_3b4fd71f026a` showed the sidebar project as `Autonomous Loop`, the power card as `Stop` / `Agent loop is on`, Mission Control as `Plan: Current` and `Execution: Codex is working`, ResearchPlan block 06 `評価付きベースラインと改善モデル` as `Current`, and Activity as `Running`.
- Follow-up root cause check on 2026-07-07 after the user reported that the power button looked off while Full Auto should still be running:
  - `/api/projects/p_3b4fd71f026a` returned `current_phase="AUTONOMOUS_LOOP"`, `autonomy_mode="full_auto"`, `target_column="TARGET"`, and `primary_dataset_snapshot_id="ds_0f38bbfae59c"`.
  - `/api/projects/p_3b4fd71f026a/agent-activity` returned `state="agent_running"`, one active Codex process, and a live worker card pointing to the native model-diagnostics notebook.
  - `/api/projects/p_3b4fd71f026a/research-plan/timeline?locale=ja-JP` returned all seven ResearchPlan blocks as `done` and `current_work=null`, meaning the plan was complete while Codex was still active in post-plan follow-up/reporting. Mission Control had been mapping that "Codex running but no declared plan node" state to `waiting`, which could show contradictory `Codex is working` and `Waiting` labels. Mission Control now maps that state to `active` and keeps the human-facing text focused on the undeclared plan position rather than implying a stopped loop.
  - `npm --prefix apps/frontend run build`: passed after the Mission Control status fix; Vite still reports the existing large chunk warning.
- Pipeline bundle packaging and download cleanup now exclude validation/runtime cache files from both newly registered bundles and existing bundle downloads. Live download for `run_82c7a25c071d` returned only `README.md`, model files, `pipeline_manifest.json`, `predict.py`, `requirements.txt`, and `train.py`; no `.tablex_smoke`, `__pycache__`, `.pyc`, or `.pyo` members were present.
  - `.venv/bin/python -m py_compile apps/backend/tabular_harness/api/routes.py apps/backend/tabular_harness/services/agent_sessions.py apps/backend/tests/test_api_flow.py apps/backend/tests/test_agent_sessions.py`: passed.
  - `.venv/bin/pytest apps/backend/tests/test_api_flow.py -q -k "pipeline_bundle_download_omits_validation_cache_files or leaderboard_reconciles_existing_run_into_chat_and_plan_links or pilot_phase_vertical"`: `3 passed, 1 warning`.
  - `.venv/bin/pytest apps/backend/tests/test_agent_sessions.py -q -k "pipeline_request_registers_prediction_pipeline_and_links_run or prediction_pipeline_worker_passes_history_for_time_series_features"`: `2 passed`.
- The stale development pair on `5181/8021` was stopped because it was serving a different database and could make the browser show old project state after the current source was fixed. The only remaining local UI/API pair for this checkpoint is `5173/8000`; `/health` returns `{"status":"ok"}`, the frontend responds, and `/agent-activity` for `p_3b4fd71f026a` still reports `agent_running` with one observed Codex process.

## Overall Status

The codebase has substantial implementation and test coverage for the directive, but the original goal is not complete until the product-level Full Auto scenario is verified end-to-end:

1. Create a new project with time-aware data.
2. Turn on Full Auto and let the main Codex session continue.
3. Verify Chat, Raw, Activity, Research Plan, native marimo notebooks, research findings, leaderboard, pipeline bundle download, pilot scoring, and follow-up iteration all remain coherent without hidden stops or maker-facing leakage.
