# 0118 Full Auto Supervisor Hardening Goal

## Goal

Make the main Full Auto Codex session harder to lose, easier to observe, and less likely to degrade into silent stops or opaque pending states.

## Context Read

- `AGENTS.md`
- `docs/agent_interface_spec.md`
- `apps/backend/tabular_harness/services/agent_sessions.py`
- `apps/backend/tabular_harness/services/agent_chat.py`
- `apps/backend/tabular_harness/services/agent_response_composer.py`
- `apps/backend/tabular_harness/services/analysis_notebooks.py`
- `apps/backend/tabular_harness/api/routes.py`
- `apps/backend/tests/test_agent_sessions.py`
- `apps/backend/tests/test_api_flow.py`

## Implemented Scope

- Removed the misleading product-generated notebook validation name and `is_tablex_generated` flag. Notebook capture eligibility is based on marimo structure, not fixed Tablex prose or markers.
- Added a regression test that Codex-authored marimo notebooks do not require product markers.
- Added artifact query indexes for large local workspaces in the prior milestone and verified existing SQLite index creation.
- Changed Codex CLI turn streaming so stdout/stderr are written first to workspace raw transcript files, while Tablex tails those files into transcript events. This reduces pipe-loss risk and keeps Raw grounded in the saved runner output.
- Added fake-Codex tests covering file-backed stdout/stderr, transcript event ingestion, thread id capture, and process-exit recording.
- Confirmed user chat instructions are delivered by undelivered event index, not by a small recent-log window.
- Added a regression test ensuring Chat response briefs include recent conversation turns.
- Added `/health` as a public alias for `/healthz` and updated Vite proxy/docs.
- Added supervisor-side progress-update nudging during Codex CLI turns. This writes `.tablex/inbox/progress_request.md` in the user's locale when the human-facing Chat update is stale, without interrupting Codex or depending on an open browser.
- Added Notebook HTML preview loading and slow-render states so a marimo/HTML preview no longer looks like a silent blank panel while the iframe is opening.
- Hardened Agent Chat history pairing so user instructions are matched to main-session progress updates by normalized datetime ordering, not raw timestamp string comparison.
- Changed queued Agent Chat copy so messages delivered to the main Codex session say that the agent has the instruction and the next Codex reply will be saved, rather than leading with worker-waiting implementation language.
- Marked main-session-delivered Chat jobs as `waiting_for_agent` instead of runnable `queued` work so the main Codex session remains the response source; the job is completed when a Codex-authored `reports/chat_update.md` is registered.
- Updated the frontend so `waiting_for_agent` chat turns remain visibly active in Chat, use the Codex-delivery message returned by the backend, stay out of Research Plan task blocks, and show localized Jobs labels instead of raw status strings.
- Rendered HTML notebook previews from the preview API's inlined/reset HTML via `srcDoc` and added an explicit empty-preview warning so blank panels are distinguishable from loading or rendering.
- Added browser-download URLs for the main AgentSession raw stdout JSONL and stderr log so Raw can show a bounded tail while still giving direct access to the complete saved transcript files.
- Tightened Research Plan locale display so unlocalized Codex-authored block strings do not leak into non-English UI while the active session receives a display-language refresh request.
- Polished Japanese Home/Agent Workspace labels that remained fixed in English after the ResearchPlan locale pass, including workflow status chips, Mission Control status labels, focus action labels, target display, ResearchPlan download affordance, and Home skill tooltip leakage.
- Preserved the main AgentSession observation on paired Chat turns after a Codex-authored `reports/chat_update.md` answers a pending user instruction, so the saved Chat turn remains traceable back to Raw.
- Added a dedicated `tablex-agent-supervisor` process entrypoint and an API setting (`TABLEX_API_AGENT_SESSION_SUPERVISOR_ENABLED`) so Full Auto session recovery can be moved out of the FastAPI process when desired.
- Added an optional Compose stack that runs the API, sidecar worker, and dedicated AgentSession supervisor as separate processes sharing the same local `/data` volume.
- Kept Codex-authored Research Plan display locale-aware on the frontend by using the timeline API `response_locale` and absorbing Codex-authored aliases for the fixed upload/objective/understanding/prior-research anchors instead of showing duplicated English anchor blocks.
- Added structured Chat wait observations for delivered-to-main-session turns so the pending Chat card can show the last Codex output age, last Chat update age, whether a progress request has been sent, and raw transcript counts while still waiting for a Codex-authored `reports/chat_update.md`.
- Localized the floating Agent Activity rail title and minimize/expand affordance so the activity overlay no longer leaves fixed English control labels in Japanese UI.
- Localized high-visibility Insight / decision-report / Notebook Center shelves and table affordances so Japanese UI no longer drops back to English around the report reader and notebook surface.
- Hardened the floating Agent Activity rail so the same job/session is rendered as one card, cards without backend project names still show the current project, the live-work pill includes the visible card count, and `waiting_for_agent` remains visible as an active wait state instead of disappearing after the transient TTL.
- Hardened the backend Agent Activity contract so `waiting_for_agent` chat turns delivered to the main Codex session are active worker cards at the API level, not only a frontend interpretation.
- Hardened Research Plan locale continuity so Codex-added timeline blocks do not leak an older English response into a Japanese UI; the timeline API now falls back to the latest explicit project/chat/user locale when no query locale is provided, and the Home UI drops stale timeline responses whose `response_locale` does not match the current display locale.
- Aligned Agent Activity card identity across backend and frontend: duplicate events for the same worker/session are merged before `active_count` and turn-state calculation, while distinct worker IDs under the same job remain separate cards for future subagent fan-out.
- Adjusted Research Plan focus selection so Mission Control and the auto-centered timeline do not jump past unresolved earlier anchors when Codex has appended a later active project-specific block. The Codex-authored block remains visible and active, but the human-facing focus first surfaces the earlier pending/blocked/waiting block.
- Removed the remaining FastAPI `BackgroundTasks` dependency from the legacy approval-based autonomy start path. That start path still returns an immediate queued control record, but the follow-up work now runs in a named daemon thread rather than being attached to the HTTP background-task lifecycle.
- Tightened Research Plan display locale continuity on the Home UI: stale timeline responses are ignored at state-update time, and initial anchor evidence no longer leaks English plural labels or internal artifact names into Japanese display.
- Bridged Raw-to-Chat observability for pending main-session replies: the Chat wait observation now carries the latest saved Codex message excerpt from the Raw transcript, and the UI renders it as observed Codex output without fabricating a replacement assistant answer.
- Added regression coverage that pending Chat history refreshes the observed latest Codex message when Raw transcript events advance, so the wait card does not freeze at the first POST response.
- Hardened Codex-added Research Plan locale handling further: partially localized timeline blocks are hidden behind a single locale-refresh block instead of leaking mixed English/Japanese text, and active sessions receive a new refresh request whenever the same ResearchPlan artifact gains a new missing-locale issue signature.
- Tightened Research Plan display-locale validation in both directions: Japanese UI no longer leaks English Codex-added plan prose, and English UI no longer leaks Japanese plan prose from older artifacts. Mismatched blocks stay hidden behind the locale-refresh state until Codex rewrites the active-locale display fields.
- Moved Approach helper POST endpoints for adaptive strategy brief, controlled research plan, and agent-task contract planning off inline HTTP execution. They now enqueue real worker jobs with concrete handlers, preserving the same job output shape after the worker completes while avoiding another request-time execution step in the Agent Workspace path.
- Connected Research Plan display-locale refresh requests to the main AgentSession wake path. If Codex is between turns and a non-English timeline request creates or observes a locale-refresh issue, Tablex records a wake event keyed by the issue signature and starts the supervisor once so Codex can process `.tablex/inbox/research_plan_locale_request.md`.
- Moved `/api/projects/{project_id}/baseline/run` off inline HTTP execution. It now validates the approved EvaluationSpec/SplitManifest boundary and returns a queued `run_baseline` worker job, matching model-candidate training and avoiding another request-time training step in the UI.
- Moved analysis notebook execution plan/capture endpoints off inline HTTP execution. They now queue `plan_notebook_execution` and `capture_notebook_execution` worker jobs; the worker owns marimo/static capture and returns the same manifest/report/HTML/source artifact contract after completion.
- Moved result-notebook evidence preparation and Decision Report generation off inline HTTP execution. These report/Notebook surfaces now return queued worker jobs while the worker registers the same authoring briefs, reports, evidence, and decision-report bundle artifacts.
- Moved additional result/reporting POST endpoints off inline HTTP execution: visualization dashboard generation, insight generation, decision dashboard generation, experiment comparison, run report drafting, evaluation diagnostics, model diagnostics artifact materialization, and model package validation now enqueue concrete worker jobs. The worker owns the same artifact/report/insight/validation output contracts after the request returns.
- Normalized frontend locale-language detection with the backend so `ja-JP`, `Japanese`, and dynamic labels beginning with `日本語` all resolve to Japanese. This prevents Codex-added Research Plan display guards from letting English plan text through after the initial Japanese anchors.
- Moved AgentTaskContract execution endpoints off inline HTTP execution. Workspace preparation, readiness review, local stub execution, and Codex CLI execution now return queued jobs, with concrete worker handlers preserving the same artifact/report/output contracts after execution.
- Moved benchmark network/download endpoints off inline HTTP execution. Public benchmark archive download and Kaggle probe, inventory, and selective download now return queued jobs; concrete worker handlers keep credentials harness-only and preserve the existing safe artifact manifests.
- Moved evaluation workflow endpoints off inline HTTP execution. Evaluation candidate design, scenario comparison, approval review, and SplitManifest generation now return queued jobs; worker handlers preserve the existing output contracts and SplitManifest generation continues to queue downstream training jobs.
- Moved Data Understanding quality and EDA review endpoints off inline HTTP execution. `/api/datasets/{dataset_id}/quality/run` and `/api/datasets/{dataset_id}/eda-review` now return queued worker jobs; concrete handlers preserve the same artifact/report/insight output contracts after completion.
- Tightened ResearchPlan display-locale continuity for Codex-added blocks. Backend locale normalization now treats human Japanese labels such as `Japanese / 日本語` as Japanese, and the Project UI uses the resolved active display locale for ResearchPlan, Strategy, Chat, Autonomy, and tab rendering instead of the raw settings value.
- Extracted dataset profiling artifact registration from `routes.py` into `services/dataset_profile.py` and made `profile_dataset` a concrete worker handler. `/datasets/{dataset_id}/profile`, `/projects/{project_id}/understanding/run`, and `/projects/{project_id}/assumptions/infer` now return queued worker jobs instead of completing inside the HTTP request.
- Hardened Codex-added ResearchPlan localization key handling. Timeline display now treats human-authored locale aliases such as `Japanese`, `日本語`, and `Japanese / 日本語` as Japanese localization keys, so Codex can provide localized plan display fields without being forced into a single exact key spelling and without falling back to English canonical text.
- Tightened Agent Chat accountability display so the pending Chat card no longer shows a Raw-derived latest Codex message when that message does not match the active display language. Raw still exposes the underlying transcript, while Chat shows a localized "Raw has new output; waiting for the human-facing update" state instead of leaking an untranslated runner utterance.

## 2026-07-04 Fable Feedback Audit

| Fable item | Current status | Evidence |
| --- | --- | --- |
| Startup recovery must not depend on an open browser | Implemented | FastAPI lifespan and the local worker daemon call `start_active_main_session_supervisors`; `test_startup_supervisor_recovers_full_auto_project_without_browser_polling` and worker-daemon recovery tests cover this. |
| Unmonitored Codex PIDs after restart must not suspend Full Auto forever | Implemented | Supervisor marks stale stored PIDs as `between_turns`, clears the PID, records `stale_runner_process_recovered`, and resumes the same session. |
| Runner failure must not hot-loop | Implemented | `runner_unavailable` and non-zero exits schedule bounded retry delays via `retry_delay_seconds`; tests cover retry counting without sidecar-event inflation. |
| Idle timeout must not repeatedly SIGTERM the same process | Implemented | Streaming loop tracks `timeout_sent`, `cancel_sent`, and `terminated_at`, sends terminate once, then kills after a grace period if needed. |
| User chat instructions must not fall out of a recent-event window or disappear after a failed turn | Implemented | `undelivered_user_instruction_events` uses the last successfully delivered user event index; the prompt includes all undelivered instructions, and the delivery watermark advances only after a successful Codex CLI turn. Regression coverage verifies a non-zero turn keeps the instruction in the next prompt. |
| Workspace artifact ingestion must avoid stem-only name collisions | Implemented | Session output artifact names include sanitized workspace-relative paths; regression coverage prevents alternating `summary.md` collisions. |
| Raw transcript storage must scale beyond small logs | Implemented for current Raw/Chat path | `(session_id, event_index)` indexes exist, stream events are batched, the transcript API supports `since_index`, and the Home activity poll uses that delta path after the initial load. The Raw file viewer separately reads bounded stdout/stderr tails. |
| Raw transcript view must expose the actual saved Codex output, not only derived event cards | Implemented | `/agent-session/raw-transcript` now returns download URLs for full stdout JSONL and stderr log files; the Raw UI exposes those links while keeping the bounded parsed tail in view. |
| Agent Chat should not synchronously block on Codex response composition | Implemented for product path | `/agent-chat` creates an `agent_chat_turn` job and immediately returns a visible wait state; the local daemon processes concrete chat jobs and history replaces pending entries with persisted responses. |
| Autonomy start should not depend on FastAPI BackgroundTasks | Implemented | Full Auto starts the main AgentSession directly, and the legacy approval-based start path now launches its follow-up tick in a named daemon thread instead of `BackgroundTasks`; tests poll the job state rather than relying on TestClient background-task completion. |
| Chat should remember recent conversation | Implemented | `build_agent_conversation_context` includes recent `agent_chat_turn` artifacts; regression test covers a second turn seeing the first. |
| Main-session Chat updates should attach to the right user instruction | Implemented | Pairing now compares normalized UTC datetimes and has regression coverage for local-time strings that would sort incorrectly as raw strings. |
| Agent Chat pending state should not look like a dead worker when Codex already has the message | Implemented | Delivered-to-session chat turns now use human-facing "delivered to agent / next Codex reply will be saved" copy while keeping worker state in structured metadata. |
| Delivered Chat instructions should not be answered by a sidecar before main Codex replies | Implemented | Active-session chat jobs use `waiting_for_agent`, which is outside `RUNNABLE_STATUSES`, and are completed by `main_codex_session_chat_update`; regression coverage verifies the local worker skips them and the Codex-authored update pairs back to the same job. |
| Agent Chat should wake the continuing main session when it is between turns | Implemented | A user message appended to a `between_turns` main AgentSession starts the session supervisor after the request is acknowledged, so the message does not wait for a later Activity poll or external supervisor scan. |
| Baseline training should not run synchronously inside the API request | Implemented | `/baseline/run` now queues a `run_baseline` worker job with the same evaluation/split inputs; the integration test asserts the endpoint returns `queued` and the worker later produces the same metrics, run, model version, and leaderboard outputs. |
| Notebook execution capture should not run synchronously inside the API request | Implemented | `/analysis-notebooks/{artifact_id}/execution-plan` and `/execution-capture` now queue concrete worker jobs; a regression test verifies the worker later creates the execution plan, manifest, HTML preview, report, and source artifacts. |
| Report and result-notebook evidence generation should not run synchronously inside the API request | Implemented | `/results/notebook-evidence` and `/decision-report/generate` now queue concrete worker jobs; integration coverage verifies the worker produces the same readout/notebook evidence and decision-report artifacts. |
| Visualization, insight, decision-dashboard, run-report, diagnostics, and validation POSTs should not run synchronously inside the API request | Implemented | `/visualizations/generate`, `/insights/generate`, `/decision-dashboard/generate`, `/experiments/compare`, `/runs/{run_id}/report`, `/runs/{run_id}/diagnostics`, `/runs/{run_id}/model-diagnostics-artifacts`, and `/model-versions/{model_version_id}/validate` now return queued jobs. Concrete worker handlers preserve the existing output contracts and focused integration tests run those jobs explicitly. |
| AgentTaskContract execution should not run synchronously inside the API request | Implemented | `/agent-task-contracts/{artifact_id}/prepare-workspace`, `/readiness-review`, `/run-local-stub`, and `/run-codex` now enqueue worker jobs. Focused integration tests run those jobs explicitly and verify the same workspace manifest, readiness report, ingested agent artifacts, relational context, and blocked-readiness failure behavior. |
| Benchmark and Kaggle network/download actions should not run synchronously inside the API request | Implemented | `/benchmarks/{benchmark_id}/public-download`, `/kaggle/probe`, `/kaggle/inventory`, and `/kaggle/download` now enqueue worker jobs. Worker handlers write the same public download, Kaggle credential probe, inventory, and selective-download manifest artifacts without materializing connector credentials into runner-visible artifacts. |
| Evaluation workflow actions should not run synchronously inside the API request | Implemented | `/evaluation/design`, `/evaluation/compare`, `/evaluation-specs/{spec_id}/approval-review`, and `/evaluation-specs/{spec_id}/generate-split` now enqueue worker jobs. Integration coverage runs those jobs explicitly and verifies candidates, scenario comparison artifacts, approval-review blockers, SplitManifest payloads, and downstream training queue creation. |
| Data Understanding quality/EDA actions should not run synchronously inside the API request | Implemented | `/datasets/{dataset_id}/quality/run` and `/datasets/{dataset_id}/eda-review` now enqueue worker jobs. Integration coverage runs those jobs explicitly and verifies quality gates, EDA bundles, HTML previews, reports, figure artifacts, and analysis-story integration. |
| Data profiling / understanding rerun actions should not complete inside the API request | Implemented | `profile_dataset` and `infer_assumptions` are concrete worker handlers; `/datasets/{dataset_id}/profile`, `/projects/{project_id}/understanding/run`, and `/projects/{project_id}/assumptions/infer` enqueue jobs. Integration coverage runs them explicitly and verifies DatasetSnapshot/profile output and assumption fallback output. |
| Delivered Chat wait state should not leak as a raw job status in UI | Implemented | The frontend treats `waiting_for_agent` as an active Chat turn, renders localized status labels, suppresses those chat wait jobs from Research Plan blocks, and uses the backend's Codex-delivery message instead of a generic pending line. |
| Codex-authored marimo notebooks must not require fixed product markers | Implemented | Capture validation checks marimo structure rather than `Generated by Tablex` style markers. |
| Research Plan after anchors must be Codex-authored and locale-aware | Implemented with active follow-up | UI reads `research_plan.v1` `timeline_blocks`; non-English locales suppress unlocalized text and request Codex to refresh visible fields in the selected locale. |
| Home/ResearchPlan Japanese UI should not revert to fixed English labels around Codex-authored plan blocks | Implemented with browser evidence | Playwright Japanese Home snapshots verified localized workflow chips, mission action, target display, ResearchPlan summary/download affordance, and collapsed locale-refresh blocks. |
| Codex-added Research Plan blocks should respect the active display locale | Implemented with latest frontend/backend fix | The frontend validates block/subtask display text against the current User Settings locale, not a stale artifact locale. When `/research-plan/timeline` detects unlocalized Codex blocks, Tablex writes both a locale-refresh inbox request and a refreshed `.tablex/context.json` so the active session sees the current display language before extending the plan. A partially localized block is now treated as not display-ready, refresh requests are keyed by a missing-locale issue signature, and a dormant main AgentSession is woken once per issue signature so Codex can process the repair request. |
| Dynamic Japanese locale labels should not make the frontend treat the UI as non-Japanese | Implemented | `localeLanguage` now mirrors the backend locale normalization for `Japanese` and `日本語...` labels, and `localePrefersShiftEnter` also recognizes Japanese-script locale labels. |
| Paired Agent Chat turns must remain traceable to Raw after the pending state is replaced | Implemented | The paired Chat history response now carries `agent_session_observation` in `response_brief`; regression coverage asserts the session id and observation schema persist after the Codex-authored update is paired. |
| Pending Chat turns should show whether Tablex has nudged Codex for a human-facing update | Implemented | Active Chat wait cards now include last Chat update age, progress-request-sent metadata, raw transcript counts, and the latest Codex message excerpt. The observation uses `AgentTranscriptEvent` when available and falls back to the saved Codex stdout JSONL when DB event ingestion is missing. Raw transcript mtime also contributes to `last_codex_output_at`, so Chat does not show a fresh Raw message with an empty/stale output clock. |
| Agent Activity should not duplicate or hide active/waiting work cards | Implemented | The frontend now deduplicates worker cards by job/session identity, preserves richer telemetry while merging duplicates, fills the current project name when missing, and treats `waiting_for_agent` as an active wait state. |
| Main-session Chat waits should stay visible in Agent Activity | Implemented | `job_active_for_activity` now treats `waiting_for_agent` as active, and the active-session chat regression asserts `/agent-activity` returns the waiting chat job as an active worker and increments `active_count`. |
| Agent Activity active count should match the visible worker-card grain | Implemented | The backend merges worker events before `active_count`, `turn_state`, and visible-card filtering; the identity keeps separate `worker_id`s under one job distinct so subagents can fan out visually. |
| Codex-added Research Plan blocks should not switch language mid-plan | Implemented | `/research-plan/timeline` uses the latest explicit project locale when needed, unlocalized Codex blocks are hidden behind the locale-refresh state, the Home UI ignores stale timeline responses from a different display locale, and the fixed anchors show locale-safe evidence labels instead of English counts or internal artifact names. |
| Research Plan surrounding strategy copy should not switch language mid-plan | Implemented | `AdaptiveStrategyBrief` now carries `response_locale` plus display-only localized labels/reasons for harness-owned action and lane chrome. The frontend requests the brief with User Settings locale, posts that locale for saved snapshots, and renders `display_*` fields before canonical English fields. Codex-authored ResearchPlan blocks remain Codex-owned and are still handled by the locale-refresh contract. |
| Research Plan should not visually skip unresolved anchor work | Implemented | Mission Control and timeline auto-centering now choose an earlier pending/blocked/waiting block before a later active Codex-added block, without removing or gating the Codex-authored block. |
| Chat/Raw latest-log visibility must be predictable | Implemented in UI | Chat and Raw sticky-bottom scroll state resets per project/session while preserving manual scroll-up within the same view. The Raw view highlights the latest merged stdout/stderr tail line instead of assuming stdout is always the newest stream. |
| Long-running Codex turns should still be prompted to explain progress | Implemented with non-blocking nudge | The supervisor now writes `.tablex/inbox/progress_request.md` during active Codex turns when the latest Codex-authored Chat update is stale; tests verify project locale and no browser polling dependency. |
| Notebook preview state should not look blank or broken while rendering | Implemented in UI | HTML/SVG preview iframes now show localized loading and slow-render states before `onLoad`, with the existing open-original fallback still available. |
| Notebook preview should not depend on a second iframe fetch for already-prepared HTML | Implemented in UI | Non-truncated HTML previews now render the API-provided inlined/reset HTML through `srcDoc`; empty previews show a visible warning and source/original fallbacks. |
| Notebook workspace chrome should respect the active locale | Implemented with browser evidence | The Notebook focus, Analysis Story, preview controls, supporting shelf, action buttons, and table chrome now use `LocaleMessages`; Firefox Playwright confirmed the Japanese UI chrome while leaving Codex-authored notebook/story content and marimo static UI untouched. |
| A supervisor can run out-of-process instead of depending on the API or generic worker | Implemented as an operator entrypoint and local Compose stack | `tablex-agent-supervisor` runs only AgentSession recovery/continuation. API-side supervisor startup can be disabled with `TABLEX_API_AGENT_SESSION_SUPERVISOR_ENABLED=false`, while the DB lease still prevents duplicate ownership. `docker-compose.yml` wires API, sidecar worker, and supervisor as separate local processes. |
| Stored runner PID after restart should not make Agent state ambiguous | Implemented | Startup recovery now clears dead stored Codex PIDs before launch, skips stale-PID noise while a local supervisor slot or cross-process lease is active, and the supervisor records distinct recovery events for dead PID remnants versus live unobserved runner processes. |
| User-triggered notebook/report/strategy preparation should not run inside HTTP request handlers | Implemented | Normal UI POSTs for generic Notebook authoring briefs, Data Understanding notebook authoring preparation, Baseline strategy planning, Project report drafts, and per-run model diagnostics notebook authoring now return queued jobs. Concrete worker handlers perform the artifact creation and preserve the previous output shapes after worker completion. |
| Codex-added ResearchPlan display should not switch language because of locale key spelling | Implemented | `research_plan_timeline` now resolves Japanese and English human locale aliases inside `localizations` / `human_display` / related display containers. Focused service and API tests cover alias-keyed Japanese plan blocks and subtasks. |
| Pending Chat should not leak Raw Codex prose in the wrong display language | Implemented | The frontend now locale-checks the latest Raw-derived Codex message before rendering it inside Chat. Mismatched text remains available in Raw, but the Chat wait card stays in the user's active display language. |
| Public benchmark workflow should not run model/report work inside the HTTP request | Implemented | `/api/projects/{project_id}/benchmarks/{benchmark_id}/public-workflow` now validates that the catalog entry is credential-free, creates a queued `run_public_benchmark_workflow` job, and returns immediately. A concrete worker handler owns download, import, evaluation, split, baseline, diagnostics, reporting, insights, and benchmark scenario artifacts. |
| Benchmark scenario/evidence and relational feature helpers should not run inside HTTP requests | Implemented | Scenario-pack, benchmark collection-plan, benchmark evidence-pack, relational feature-plan, relational recipe build, and relational scenario diagnostics endpoints now return queued worker jobs. Concrete worker handlers preserve the prior artifact/report/evidence output contracts after execution. |
| Local benchmark import and fixture smoke workflows should not run inside HTTP requests | Implemented | `/benchmarks/{benchmark_id}/import` and `/fixture-smoke` now return queued worker jobs. Worker handlers own local file validation, fixture generation, dataset profiling, relational catalog/supporting-table artifacts, evaluation smoke setup, and scenario artifacts while preserving the previous output payload after completion. |
| Guided Journey artifact creation should not run inside HTTP requests | Implemented | Guidance snapshot, autonomous decision brief, and guided-journey comparison endpoints now return queued worker jobs. Concrete worker handlers own report/artifact/visualization creation and preserve the previous output payloads after completion. |
| Relational schema hint upload should not parse/report inside the HTTP request | Implemented | `/relational/schema-hints/upload` now stores the uploaded ER/JSON file as a staging artifact and returns a queued worker job. The worker materializes the relational schema hint artifact, report, evidence, and lineage while preserving the previous output payload after completion. |
| Codex-added ResearchPlan blocks should use the timeline response locale in the UI | Implemented | The Home timeline now renders Codex-authored blocks against the timeline API `response_locale` / `requested_locale` / `authored_locale` chain, so a Japanese UI does not fall back to English when the Codex plan artifact and display settings use aliases or repaired locale metadata. |

## Deferred Scope

- Do not prune historical duplicate artifacts automatically. Existing projects may still contain large artifact histories from older naming behavior; deletion or compaction needs an explicit maintenance command and user approval.
- systemd/supervisord unit files remain future work. Docker Compose wiring is present for local split-process operation.
- Full browser UX review remains necessary for Activity overlay behavior under live running workloads. Chat/Raw, Research Plan Japanese rendering, and Notebook preview/Japanese chrome now have Firefox Playwright evidence for the inspected project.
- Chat quality still depends on Codex honoring `reports/chat_update.md` at a useful cadence. The harness now requests those updates from the supervisor path as well as user/chat/activity paths, but real long-running sessions should be observed to confirm the cadence feels natural.
- The product still has large frontend/backend files (`main.tsx`, `routes.py`, `analysis_notebooks.py`). Future edits should reduce risk through extraction rather than broad, unrelated edits.

## Risks

- File-backed stdout/stderr depends on Codex CLI flushing JSONL promptly. The final drain captures output at process exit, but live UI updates could lag if the CLI buffers heavily.
- The Home Raw view intentionally keeps only the recent indexed event window in React state while raw stdout/stderr tail remains available from workspace files. This is the right default for local scale, but a future dedicated transcript viewer may need virtualized history pagination.
- Historical sessions still contain old transcript/artifact records and may not visually represent the improved path until new turns run.
- `apps/frontend/src/main.tsx` remains very large and risky to edit. Future UI work should extract components deliberately instead of continuing broad edits in one file.

## Verification

- `.venv/bin/pytest apps/backend/tests -q`
- `.venv/bin/ruff check apps/backend`
- `npm run lint`
- `npm run build`
- Playwright CLI Japanese Home snapshot for `#/projects/p_9f25dd620d8c` after locale polish
- Live checks after restart:
  - `curl http://127.0.0.1:8000/health`
  - `curl http://127.0.0.1:8000/healthz`
- 2026-07-04 follow-up:
  - `npm run lint` in `apps/frontend`
  - `npm run build` in `apps/frontend`
  - `.venv/bin/pytest apps/backend/tests/test_api_flow.py::test_agent_chat_writes_active_session_instruction_to_workspace_inbox apps/backend/tests/test_api_flow.py::test_agent_chat_history_pairs_main_session_update_to_delivered_instruction apps/backend/tests/test_api_flow.py::test_agent_chat_history_pairs_each_main_session_update_once -q`
  - `git diff --check`
  - Playwright CLI Firefox browser check for `#/projects/p_9f25dd620d8c` with `tablex.userSettings.v1.locale=ja-JP`.
  - Verified the Japanese Home snapshot renders localized project status chips, Mission Control, Research Plan anchors, collapsed display-language refresh block for unlocalized Codex-added plan blocks, Chat input-ready state, and Raw stdout/stderr download links plus JSONL tail.
  - Verified the Japanese Insight snapshot renders localized decision report labels, evidence reader action labels, supporting report shelf controls, Notebook Center empty state, and lower report/notebook shelf empty states.
  - Saved visual evidence outside git at `output/playwright/fable-hardening-chat-ja-firefox.png`, `output/playwright/fable-hardening-raw-ja-firefox.png`, and `output/playwright/fable-hardening-insight-ja-firefox.png`.
  - Console error check reported 0 browser errors.
  - Activity overlay follow-up: `npm run lint`, `npm run build`, `curl http://127.0.0.1:8000/health`, Playwright Firefox reload for `#/projects/p_9f25dd620d8c`, and console error check reported 0 browser errors.
  - Backend Activity contract follow-up: `.venv/bin/pytest apps/backend/tests/test_api_flow.py::test_agent_chat_writes_active_session_instruction_to_workspace_inbox apps/backend/tests/test_api_flow.py::test_agent_activity_turn_state_waits_for_user_when_no_agent_work_is_observed apps/backend/tests/test_api_flow.py::test_agent_activity_does_not_show_future_autonomous_heartbeat_as_active -q`, `.venv/bin/ruff check apps/backend`, `npm run lint`, and `npm run build`.
  - Research Plan locale continuity follow-up: `.venv/bin/pytest apps/backend/tests/test_api_flow.py::test_research_plan_timeline_reads_artifact_authored_blocks apps/backend/tests/test_api_flow.py::test_research_plan_timeline_requests_locale_refresh_for_active_session apps/backend/tests/test_api_flow.py::test_research_plan_timeline_uses_artifact_locale_and_codex_display_fields apps/backend/tests/test_api_flow.py::test_research_plan_timeline_refresh_uses_artifact_locale_when_query_locale_absent apps/backend/tests/test_api_flow.py::test_research_plan_timeline_defaults_to_latest_project_locale -q`, `.venv/bin/ruff check apps/backend`, `npm run lint` in `apps/frontend`, `npm run build` in `apps/frontend`, and `git diff --check`.
  - Agent Activity card-grain follow-up: `.venv/bin/pytest apps/backend/tests/test_api_flow.py::test_visible_activity_workers_hide_old_terminal_cards apps/backend/tests/test_api_flow.py::test_merge_activity_workers_keeps_subagent_cards_distinct apps/backend/tests/test_api_flow.py::test_agent_chat_writes_active_session_instruction_to_workspace_inbox apps/backend/tests/test_api_flow.py::test_agent_activity_turn_state_waits_for_user_when_no_agent_work_is_observed apps/backend/tests/test_api_flow.py::test_agent_activity_does_not_show_future_autonomous_heartbeat_as_active -q`, `.venv/bin/ruff check apps/backend`, `npm run lint` in `apps/frontend`, `npm run build` in `apps/frontend`, and `git diff --check`.
  - Research Plan focus follow-up: `npm run lint` in `apps/frontend`, `npm run build` in `apps/frontend`, and `git diff --check`.
  - Autonomy Start BackgroundTasks removal follow-up: `.venv/bin/pytest apps/backend/tests/test_api_flow.py::test_approval_based_start_creates_real_planning_evidence_with_dataset apps/backend/tests/test_api_flow.py::test_approval_based_start_runs_but_does_not_auto_adopt_evaluation apps/backend/tests/test_api_flow.py::test_full_auto_start_creates_main_agent_session_without_dataset_even_with_legacy_runner_mode -q`, `.venv/bin/ruff check apps/backend`, and `git diff --check`.
  - Research Plan display-locale evidence follow-up: `.venv/bin/pytest apps/backend/tests/test_api_flow.py::test_research_plan_timeline_reads_artifact_authored_blocks apps/backend/tests/test_api_flow.py::test_research_plan_timeline_requests_locale_refresh_for_active_session apps/backend/tests/test_api_flow.py::test_research_plan_timeline_uses_artifact_locale_and_codex_display_fields apps/backend/tests/test_api_flow.py::test_research_plan_timeline_refresh_uses_artifact_locale_when_query_locale_absent apps/backend/tests/test_api_flow.py::test_research_plan_timeline_defaults_to_latest_project_locale -q`, `npm run lint` in `apps/frontend`, `npm run build` in `apps/frontend`, and `git diff --check`.
  - Pending Chat latest-Codex-output follow-up: `.venv/bin/pytest apps/backend/tests/test_api_flow.py::test_agent_chat_writes_active_session_instruction_to_workspace_inbox apps/backend/tests/test_api_flow.py::test_agent_chat_history_pairs_main_session_update_to_delivered_instruction apps/backend/tests/test_api_flow.py::test_agent_chat_history_pairs_each_main_session_update_once -q`, `.venv/bin/ruff check apps/backend`, `npm run lint` in `apps/frontend`, `npm run build` in `apps/frontend`, and `git diff --check`.
  - Pending Chat latest-Codex-output refresh follow-up: `.venv/bin/pytest apps/backend/tests/test_api_flow.py::test_agent_chat_writes_active_session_instruction_to_workspace_inbox apps/backend/tests/test_api_flow.py::test_agent_chat_history_pairs_main_session_update_to_delivered_instruction apps/backend/tests/test_api_flow.py::test_agent_chat_history_pairs_each_main_session_update_once -q`, `.venv/bin/ruff check apps/backend`, and `git diff --check`.
  - Research Plan active-locale context refresh follow-up: `.venv/bin/pytest apps/backend/tests/test_agent_sessions.py::test_turn_prompt_includes_living_research_plan_contract apps/backend/tests/test_agent_sessions.py::test_research_plan_ingest_requests_locale_refresh_for_mixed_language_timeline apps/backend/tests/test_api_flow.py::test_research_plan_timeline_requests_locale_refresh_for_active_session apps/backend/tests/test_api_flow.py::test_research_plan_timeline_reads_artifact_authored_blocks apps/backend/tests/test_api_flow.py::test_research_plan_timeline_uses_artifact_locale_and_codex_display_fields apps/backend/tests/test_api_flow.py::test_research_plan_timeline_refresh_uses_artifact_locale_when_query_locale_absent apps/backend/tests/test_api_flow.py::test_research_plan_timeline_defaults_to_latest_project_locale -q`, `.venv/bin/ruff check apps/backend/tabular_harness/services/agent_sessions.py apps/backend/tests/test_agent_sessions.py apps/backend/tests/test_api_flow.py`, `npm run lint` in `apps/frontend`, `npm run build` in `apps/frontend`, and `git diff --check`.
  - Raw transcript fallback follow-up: `.venv/bin/pytest apps/backend/tests/test_api_flow.py::test_agent_chat_writes_active_session_instruction_to_workspace_inbox apps/backend/tests/test_api_flow.py::test_agent_chat_wait_observation_falls_back_to_raw_codex_transcript apps/backend/tests/test_api_flow.py::test_agent_chat_history_pairs_main_session_update_to_delivered_instruction apps/backend/tests/test_api_flow.py::test_agent_chat_history_pairs_each_main_session_update_once -q`, `.venv/bin/ruff check apps/backend/tabular_harness/api/routes.py apps/backend/tests/test_api_flow.py`, `npm run lint` in `apps/frontend`, `npm run build` in `apps/frontend`, and `git diff --check`.
  - Raw output timestamp fallback follow-up: `.venv/bin/pytest apps/backend/tests/test_api_flow.py::test_agent_chat_writes_active_session_instruction_to_workspace_inbox apps/backend/tests/test_api_flow.py::test_agent_chat_wait_observation_falls_back_to_raw_codex_transcript apps/backend/tests/test_api_flow.py::test_agent_chat_history_pairs_main_session_update_to_delivered_instruction apps/backend/tests/test_api_flow.py::test_agent_chat_history_pairs_each_main_session_update_once -q`, `.venv/bin/ruff check apps/backend/tabular_harness/api/routes.py apps/backend/tests/test_api_flow.py`, and `git diff --check`.
  - User-instruction delivery watermark follow-up: `.venv/bin/pytest apps/backend/tests/test_agent_sessions.py::test_codex_cli_turn_streaming_uses_workspace_file_transcript apps/backend/tests/test_agent_sessions.py::test_codex_cli_turn_failure_does_not_mark_user_instructions_delivered apps/backend/tests/test_agent_sessions.py::test_turn_prompt_delivers_all_undelivered_user_instructions_beyond_recent_raw_window -q`, `.venv/bin/ruff check apps/backend/tabular_harness/services/agent_sessions.py apps/backend/tests/test_agent_sessions.py`, and `git diff --check`.
  - Agent Chat wake-up follow-up: `.venv/bin/pytest apps/backend/tests/test_api_flow.py::test_agent_chat_writes_active_session_instruction_to_workspace_inbox apps/backend/tests/test_api_flow.py::test_agent_chat_wakes_between_turns_main_session apps/backend/tests/test_api_flow.py::test_agent_chat_wait_observation_falls_back_to_raw_codex_transcript -q`, `.venv/bin/ruff check apps/backend/tabular_harness/api/routes.py apps/backend/tests/test_api_flow.py`, and `git diff --check`.
  - Notebook locale browser follow-up: `npm run lint` in `apps/frontend`, `npm run build` in `apps/frontend`, `curl http://127.0.0.1:8000/health`, Firefox Playwright on `#/projects/p_9f25dd620d8c` with `tablex.userSettings.v1.locale=ja-JP`, Notebook tab snapshot, and viewport screenshot `.playwright-cli/page-2026-07-03T22-35-31-417Z.png`. The embedded marimo iframe still emits upstream static-mode console warnings/errors, but the notebook HTML content renders.
  - Codex-added Research Plan locale follow-up: `.venv/bin/pytest apps/backend/tests/test_research_plan_timeline.py apps/backend/tests/test_agent_sessions.py::test_research_plan_ingest_requests_locale_refresh_for_mixed_language_timeline apps/backend/tests/test_agent_sessions.py::test_research_plan_locale_refresh_reissues_when_issue_signature_changes`, `.venv/bin/pytest apps/backend/tests/test_api_flow.py::test_research_plan_timeline_reads_artifact_authored_blocks apps/backend/tests/test_api_flow.py::test_research_plan_timeline_requests_locale_refresh_for_active_session apps/backend/tests/test_api_flow.py::test_research_plan_timeline_uses_artifact_locale_and_codex_display_fields apps/backend/tests/test_api_flow.py::test_research_plan_timeline_refresh_uses_artifact_locale_when_query_locale_absent apps/backend/tests/test_api_flow.py::test_research_plan_timeline_defaults_to_latest_project_locale apps/backend/tests/test_agent_sessions.py::test_turn_prompt_includes_living_research_plan_contract`, `npm run lint`, `npm run build`, `git diff --check`, and Firefox Playwright on `#/projects/p_9f25dd620d8c` with `tablex.userSettings.v1.locale=ja-JP`; saved screenshot `.playwright-cli/page-2026-07-03T22-47-16-455Z.png`.
  - Research Plan locale wake follow-up: `.venv/bin/pytest apps/backend/tests/test_api_flow.py::test_research_plan_timeline_requests_locale_refresh_for_active_session apps/backend/tests/test_api_flow.py::test_research_plan_timeline_wakes_between_turns_session_for_locale_refresh apps/backend/tests/test_api_flow.py::test_research_plan_timeline_defaults_to_latest_project_locale`, `.venv/bin/ruff check apps/backend/tabular_harness/api/routes.py apps/backend/tests/test_api_flow.py`, and `git diff --check`.
  - Supervisor stale PID recovery follow-up: `.venv/bin/pytest apps/backend/tests/test_agent_sessions.py::test_startup_supervisor_recovers_full_auto_project_without_browser_polling apps/backend/tests/test_agent_sessions.py::test_startup_supervisor_clears_dead_stored_pid_before_launch apps/backend/tests/test_agent_sessions.py::test_startup_supervisor_does_not_emit_stale_pid_event_when_local_slot_is_active apps/backend/tests/test_agent_sessions.py::test_startup_supervisor_does_not_emit_stale_pid_event_when_cross_process_lease_is_active apps/backend/tests/test_agent_sessions.py::test_runner_failure_backoff_counts_attempts_not_sidecar_events -q`, `.venv/bin/pytest apps/backend/tests/test_agent_sessions.py::test_supervisor_thread_releases_slot_when_global_runner_is_replaced apps/backend/tests/test_agent_sessions.py::test_startup_supervisor_recovers_full_auto_project_without_browser_polling apps/backend/tests/test_agent_sessions.py::test_startup_supervisor_clears_dead_stored_pid_before_launch apps/backend/tests/test_agent_sessions.py::test_startup_supervisor_does_not_emit_stale_pid_event_when_local_slot_is_active apps/backend/tests/test_agent_sessions.py::test_startup_supervisor_does_not_emit_stale_pid_event_when_cross_process_lease_is_active apps/backend/tests/test_agent_sessions.py::test_agent_supervisor_lease_prevents_duplicate_cross_process_owners apps/backend/tests/test_agent_sessions.py::test_supervisor_lease_heartbeat_signals_lost_lease apps/backend/tests/test_agent_sessions.py::test_main_supervisor_stops_before_runner_when_lease_is_lost apps/backend/tests/test_agent_sessions.py::test_research_plan_ingest_requests_locale_refresh_for_mixed_language_timeline apps/backend/tests/test_agent_sessions.py::test_research_plan_locale_refresh_reissues_when_issue_signature_changes -q`, `.venv/bin/pytest apps/backend/tests/test_worker_daemon.py::test_local_worker_daemon_periodically_retries_agent_session_supervisor_recovery apps/backend/tests/test_worker_daemon.py::test_api_lifespan_can_disable_agent_session_supervisor -q`, `.venv/bin/ruff check apps/backend/tabular_harness/services/agent_sessions.py apps/backend/tests/test_agent_sessions.py`, and `git diff --check`.
  - Agent Chat wait-job guard follow-up: `.venv/bin/pytest apps/backend/tests/test_api_flow.py::test_agent_chat_writes_active_session_instruction_to_workspace_inbox apps/backend/tests/test_api_flow.py::test_agent_chat_wakes_between_turns_main_session apps/backend/tests/test_api_flow.py::test_agent_chat_wait_observation_falls_back_to_raw_codex_transcript -q`, `.venv/bin/ruff check apps/backend/tabular_harness/worker/runner.py apps/backend/tests/test_api_flow.py`, and `git diff --check`.
  - Codex-authored marimo validation follow-up: `.venv/bin/pytest apps/backend/tests/test_analysis_notebooks.py -q`, `.venv/bin/pytest apps/backend/tests/test_agent_sessions.py::test_codex_authored_marimo_notebook_is_auto_captured_on_workspace_ingest apps/backend/tests/test_agent_sessions.py::test_codex_authored_marimo_notebook_capture_can_defer_until_final_ingest -q`, `.venv/bin/ruff check apps/backend/tabular_harness/services/analysis_notebooks.py apps/backend/tests/test_analysis_notebooks.py`, and `git diff --check`.
  - Research Plan display-locale render guard follow-up: `.venv/bin/pytest apps/backend/tests/test_research_plan_timeline.py -q`, `.venv/bin/pytest apps/backend/tests/test_agent_sessions.py::test_research_plan_ingest_requests_locale_refresh_for_mixed_language_timeline -q`, `.venv/bin/ruff check apps/backend/tabular_harness/services/agent_sessions.py apps/backend/tests/test_agent_sessions.py apps/backend/tests/test_research_plan_timeline.py`, `npm run lint` and `npm run build` in `apps/frontend`, and `git diff --check`.
  - Queued baseline HTTP follow-up: `.venv/bin/pytest apps/backend/tests/test_api_flow.py::test_project_upload_profile_evaluation_split_flow -q`, `.venv/bin/pytest apps/backend/tests/test_api_flow.py::test_agent_chat_records_conversation_without_mutating_project_state apps/backend/tests/test_api_flow.py::test_agent_chat_brief_includes_recent_conversation_turns -q`, `.venv/bin/pytest apps/backend/tests/test_worker_daemon.py -q`, `.venv/bin/pytest apps/backend/tests/test_api_flow.py::test_model_candidates_endpoint_queues_requested_models_into_leaderboard -q`, `.venv/bin/ruff check apps/backend/tabular_harness/api/routes.py apps/backend/tests/test_api_flow.py`, and `git diff --check`.
  - Queued notebook execution follow-up: `.venv/bin/pytest apps/backend/tests/test_api_flow.py::test_notebook_execution_endpoints_queue_worker_jobs -q`, `.venv/bin/pytest apps/backend/tests/test_analysis_notebooks.py -q`, `.venv/bin/pytest apps/backend/tests/test_worker_daemon.py -q`, `.venv/bin/ruff check apps/backend/tabular_harness/api/routes.py apps/backend/tabular_harness/worker/jobs.py apps/backend/tests/test_api_flow.py`, and `git diff --check`.
  - Queued report/readout follow-up: `.venv/bin/pytest apps/backend/tests/test_api_flow.py::test_core_harness_actions_use_explicit_endpoints -q`, `.venv/bin/pytest apps/backend/tests/test_api_flow.py::test_project_upload_profile_evaluation_split_flow -q`, `.venv/bin/pytest apps/backend/tests/test_api_flow.py::test_notebook_execution_endpoints_queue_worker_jobs -q`, `.venv/bin/pytest apps/backend/tests/test_worker_daemon.py -q`, `.venv/bin/ruff check apps/backend/tabular_harness/api/routes.py apps/backend/tabular_harness/worker/jobs.py apps/backend/tests/test_api_flow.py`, and `git diff --check`.
  - Queued result/reporting/diagnostics POST follow-up: `.venv/bin/pytest apps/backend/tests/test_api_flow.py::test_core_harness_actions_use_explicit_endpoints -q`, `.venv/bin/pytest apps/backend/tests/test_api_flow.py::test_project_upload_profile_evaluation_split_flow -q`, `.venv/bin/pytest apps/backend/tests/test_api_flow.py::test_benchmark_relational_catalog_infers_shared_keys -q`, `.venv/bin/pytest apps/backend/tests/test_worker_daemon.py -q`, `.venv/bin/pytest apps/backend/tests/test_model_diagnostics_artifacts.py apps/backend/tests/test_analysis_notebooks.py -q`, `.venv/bin/ruff check apps/backend/tabular_harness/api/routes.py apps/backend/tabular_harness/worker/jobs.py apps/backend/tests/test_api_flow.py`, and `git diff --check`.
  - AdaptiveStrategy/Research Plan surrounding-locale follow-up: `.venv/bin/pytest apps/backend/tests/test_api_flow.py::test_adaptive_strategy_brief_returns_locale_display_fields apps/backend/tests/test_research_plan_timeline.py -q`, `.venv/bin/pytest apps/backend/tests/test_api_flow.py::test_project_upload_profile_evaluation_split_flow apps/backend/tests/test_api_flow.py::test_core_harness_actions_use_explicit_endpoints -q`, `.venv/bin/pytest apps/backend/tests/test_worker_daemon.py -q`, `.venv/bin/ruff check apps/backend/tabular_harness/services/adaptive_strategy.py apps/backend/tabular_harness/api/routes.py apps/backend/tabular_harness/worker/jobs.py apps/backend/tabular_harness/schemas/api.py apps/backend/tests/test_api_flow.py`, `npm run lint` in `apps/frontend`, `npm run build` in `apps/frontend`, `.venv/bin/python -m py_compile apps/backend/tabular_harness/services/adaptive_strategy.py apps/backend/tabular_harness/api/routes.py apps/backend/tabular_harness/worker/jobs.py`, and `git diff --check`.
  - Queued notebook/report/strategy preparation follow-up: `.venv/bin/pytest apps/backend/tests/test_api_flow.py -q`, `.venv/bin/pytest apps/backend/tests/test_analysis_notebooks.py apps/backend/tests/test_model_diagnostics_artifacts.py -q`, `.venv/bin/pytest apps/backend/tests/test_worker_daemon.py -q`, `.venv/bin/ruff check apps/backend/tabular_harness/api/routes.py apps/backend/tabular_harness/worker/jobs.py apps/backend/tests/test_api_flow.py`, `.venv/bin/python -m py_compile apps/backend/tabular_harness/worker/jobs.py apps/backend/tabular_harness/api/routes.py`, and `git diff --check`.
  - Frontend locale normalization follow-up: `npm --prefix apps/frontend run build`, `.venv/bin/pytest apps/backend/tests/test_research_plan_timeline.py apps/backend/tests/test_api_flow.py -q`, `.venv/bin/pytest apps/backend/tests/test_agent_sessions.py apps/backend/tests/test_worker_daemon.py -q`, `.venv/bin/ruff check apps/backend/tabular_harness/services/agent_sessions.py apps/backend/tabular_harness/main.py apps/backend/tabular_harness/services/agent_chat.py apps/backend/tabular_harness/services/agent_response_composer.py`, and `git diff --check`.
  - ResearchPlan locale alias follow-up: `.venv/bin/pytest apps/backend/tests/test_research_plan_timeline.py -q`, `.venv/bin/pytest apps/backend/tests/test_api_flow.py::test_research_plan_timeline_reads_artifact_authored_blocks apps/backend/tests/test_api_flow.py::test_research_plan_timeline_uses_artifact_locale_and_codex_display_fields apps/backend/tests/test_api_flow.py::test_research_plan_timeline_defaults_to_latest_project_locale -q`, `.venv/bin/ruff check apps/backend/tabular_harness/services/research_plan_timeline.py apps/backend/tests/test_research_plan_timeline.py`, `npm --prefix apps/frontend run build`, and `git diff --check`.
  - Pending Chat Raw-language guard follow-up: `npm --prefix apps/frontend run build`, `.venv/bin/pytest apps/backend/tests/test_api_flow.py::test_agent_chat_wait_observation_falls_back_to_raw_codex_transcript apps/backend/tests/test_api_flow.py::test_agent_chat_history_pairs_main_session_update_to_delivered_instruction -q`, and `git diff --check`.
  - AgentTaskContract endpoint queueing follow-up: `.venv/bin/pytest apps/backend/tests/test_api_flow.py::test_core_harness_actions_use_explicit_endpoints apps/backend/tests/test_api_flow.py::test_planned_agent_task_stub_rejects_blocked_readiness apps/backend/tests/test_api_flow.py::test_relational_schema_hint_upload_preview_and_agent_route -q`, `.venv/bin/pytest apps/backend/tests/test_worker_daemon.py -q`, `.venv/bin/pytest apps/backend/tests/test_agent_sessions.py -q`, `.venv/bin/ruff check apps/backend/tabular_harness/api/routes.py apps/backend/tabular_harness/worker/jobs.py apps/backend/tests/test_api_flow.py`, `.venv/bin/python -m py_compile apps/backend/tabular_harness/api/routes.py apps/backend/tabular_harness/worker/jobs.py`, and `git diff --check`.
  - Benchmark/Kaggle endpoint queueing follow-up: `.venv/bin/pytest apps/backend/tests/test_api_flow.py::test_public_benchmark_download_extracts_expected_files apps/backend/tests/test_api_flow.py::test_public_benchmark_download_places_direct_csv -q`, `.venv/bin/pytest apps/backend/tests/test_kaggle_probe.py -q`, `.venv/bin/pytest apps/backend/tests/test_worker_daemon.py -q`, `.venv/bin/pytest apps/backend/tests/test_api_flow.py -q`, `.venv/bin/ruff check apps/backend/tabular_harness/api/routes.py apps/backend/tabular_harness/worker/jobs.py apps/backend/tests/test_api_flow.py apps/backend/tests/test_kaggle_probe.py`, `.venv/bin/python -m py_compile apps/backend/tabular_harness/api/routes.py apps/backend/tabular_harness/worker/jobs.py apps/backend/tests/test_kaggle_probe.py`, and `git diff --check`.
  - Evaluation workflow endpoint queueing follow-up: `.venv/bin/pytest apps/backend/tests/test_evaluation_splits.py -q`, `.venv/bin/pytest apps/backend/tests/test_api_flow.py::test_project_upload_profile_evaluation_split_flow apps/backend/tests/test_api_flow.py::test_evaluation_approval_blocks_required_unanswered_question apps/backend/tests/test_api_flow.py::test_core_harness_actions_use_explicit_endpoints apps/backend/tests/test_api_flow.py::test_benchmark_relational_catalog_infers_shared_keys -q`, `.venv/bin/pytest apps/backend/tests/test_worker_daemon.py apps/backend/tests/test_agent_sessions.py -q`, `.venv/bin/pytest apps/backend/tests/test_api_flow.py -q`, `npm --prefix apps/frontend run build`, `.venv/bin/ruff check apps/backend/tabular_harness/api/routes.py apps/backend/tabular_harness/worker/jobs.py apps/backend/tests/test_api_flow.py apps/backend/tests/test_evaluation_splits.py`, `.venv/bin/python -m py_compile apps/backend/tabular_harness/api/routes.py apps/backend/tabular_harness/worker/jobs.py apps/backend/tests/test_evaluation_splits.py`, and `git diff --check`.
  - ResearchPlan locale + Data Understanding queue follow-up: `.venv/bin/ruff check apps/backend/tabular_harness/api/routes.py apps/backend/tabular_harness/worker/jobs.py apps/backend/tabular_harness/services/locales.py apps/backend/tabular_harness/services/research_plan_timeline.py apps/backend/tests/test_api_flow.py apps/backend/tests/test_research_plan_timeline.py`, `.venv/bin/python -m py_compile apps/backend/tabular_harness/api/routes.py apps/backend/tabular_harness/worker/jobs.py apps/backend/tabular_harness/services/locales.py apps/backend/tabular_harness/services/research_plan_timeline.py`, `.venv/bin/pytest apps/backend/tests/test_research_plan_timeline.py apps/backend/tests/test_api_flow.py::test_research_plan_timeline_reads_artifact_authored_blocks apps/backend/tests/test_api_flow.py::test_research_plan_timeline_defaults_to_latest_project_locale apps/backend/tests/test_api_flow.py::test_core_harness_actions_use_explicit_endpoints apps/backend/tests/test_api_flow.py::test_project_upload_profile_evaluation_split_flow -q`, `.venv/bin/pytest apps/backend/tests/test_api_flow.py -q`, `.venv/bin/pytest apps/backend/tests/test_worker_daemon.py apps/backend/tests/test_agent_sessions.py -q`, `npm --prefix apps/frontend run build`, and `git diff --check`.
  - Data profiling queue follow-up: `.venv/bin/ruff check apps/backend/tabular_harness/api/routes.py apps/backend/tabular_harness/worker/jobs.py apps/backend/tabular_harness/services/dataset_profile.py apps/backend/tests/test_api_flow.py apps/backend/tests/test_worker_daemon.py`, `.venv/bin/python -m py_compile apps/backend/tabular_harness/api/routes.py apps/backend/tabular_harness/worker/jobs.py apps/backend/tabular_harness/services/dataset_profile.py`, `.venv/bin/pytest apps/backend/tests/test_api_flow.py::test_core_harness_actions_use_explicit_endpoints apps/backend/tests/test_api_flow.py::test_project_upload_profile_evaluation_split_flow apps/backend/tests/test_worker_daemon.py::test_local_worker_daemon_does_not_succeed_stub_only_jobs -q`, `.venv/bin/pytest apps/backend/tests/test_api_flow.py -q`, `.venv/bin/pytest apps/backend/tests/test_worker_daemon.py apps/backend/tests/test_agent_sessions.py -q`, `npm --prefix apps/frontend run build`, and `git diff --check`.
  - Agent/Research handoff queue follow-up: queued Research source pack, local research stub, research synthesis, research brief generation, approach idea generation, idea context preparation, experiment-plan creation, and idea agent-task execution behind worker handlers while preserving previous output shapes after job completion. Also cleared stale ResearchPlan timeline state when the response locale does not match the active User Settings locale. Verification: `.venv/bin/ruff check apps/backend/tabular_harness/api/routes.py apps/backend/tabular_harness/worker/jobs.py apps/backend/tabular_harness/services/jobs.py apps/backend/tests/test_api_flow.py apps/backend/tabular_harness/services/research_plan_timeline.py`, `.venv/bin/python -m py_compile apps/backend/tabular_harness/api/routes.py apps/backend/tabular_harness/worker/jobs.py apps/backend/tabular_harness/services/jobs.py`, `.venv/bin/pytest apps/backend/tests/test_research_plan_timeline.py apps/backend/tests/test_api_flow.py::test_research_plan_timeline_reads_artifact_authored_blocks apps/backend/tests/test_api_flow.py::test_research_plan_timeline_defaults_to_latest_project_locale apps/backend/tests/test_api_flow.py::test_adaptive_strategy_brief_returns_locale_display_fields apps/backend/tests/test_api_flow.py::test_project_upload_profile_evaluation_split_flow -q`, `npm --prefix apps/frontend run build`, and `git diff --check`.
  - Research Plan locale alias follow-up: the frontend locale pack resolver now maps language aliases such as `ja`, `Japanese`, and `日本語` back to the built-in Japanese pack instead of falling through to `en-US`. This keeps User Settings, ResearchPlan timeline requests, Strategy brief requests, and Codex-added plan display guards on the same active display language even when the server stores a human-readable locale label. Verification: `npm --prefix apps/frontend run build`, `.venv/bin/pytest apps/backend/tests/test_research_plan_timeline.py apps/backend/tests/test_api_flow.py::test_research_plan_timeline_reads_artifact_authored_blocks apps/backend/tests/test_api_flow.py::test_research_plan_timeline_defaults_to_latest_project_locale apps/backend/tests/test_api_flow.py::test_adaptive_strategy_brief_returns_locale_display_fields -q`.
  - AgentSession workspace artifact collision follow-up: added a regression test proving same-stem Codex outputs in different workspace directories, such as `reports/summary.md` and `outputs/summary.md`, register as distinct artifacts keyed by workspace-relative path and do not multiply on repeated ingest. This locks Fable A-5 against future regressions and protects long Full Auto sessions from artifact/version growth caused by natural Codex output layouts. Verification: `.venv/bin/pytest apps/backend/tests/test_agent_sessions.py::test_workspace_output_artifact_names_include_relative_path_to_avoid_stem_collisions apps/backend/tests/test_agent_sessions.py::test_codex_authored_chat_update_is_registered_as_persistent_chat_turn apps/backend/tests/test_agent_sessions.py::test_published_raw_codex_transcript_is_ingested_as_session_artifact -q`, `.venv/bin/ruff check apps/backend/tests/test_agent_sessions.py`.
  - Research Plan unassigned work visibility follow-up: the Home timeline now appends a localized observed-work block for non-terminal jobs that do not yet map to an existing Codex-authored plan block. This keeps evaluation/modeling/reporting workers visible in Research Plan before Codex has extended the living plan, without constraining Codex to fixed downstream blocks. Verification: `npm --prefix apps/frontend run build`, `.venv/bin/pytest apps/backend/tests/test_agent_sessions.py::test_workspace_output_artifact_names_include_relative_path_to_avoid_stem_collisions apps/backend/tests/test_api_flow.py::test_full_auto_codex_start_creates_main_agent_session_transcript apps/backend/tests/test_api_flow.py::test_agent_chat_writes_active_session_instruction_to_workspace_inbox -q`.
  - Public benchmark workflow queueing and ResearchPlan response-locale UI follow-up: `.venv/bin/python -m py_compile apps/backend/tabular_harness/worker/jobs.py apps/backend/tabular_harness/api/routes.py`, `.venv/bin/ruff check apps/backend/tabular_harness/worker/jobs.py apps/backend/tabular_harness/api/routes.py apps/backend/tests/test_api_flow.py`, `.venv/bin/pytest apps/backend/tests/test_research_plan_timeline.py apps/backend/tests/test_api_flow.py::test_research_plan_timeline_reads_artifact_authored_blocks apps/backend/tests/test_api_flow.py::test_research_plan_timeline_requests_locale_refresh_for_active_session apps/backend/tests/test_api_flow.py::test_research_plan_timeline_wakes_between_turns_session_for_locale_refresh apps/backend/tests/test_api_flow.py::test_research_plan_timeline_uses_artifact_locale_and_codex_display_fields apps/backend/tests/test_api_flow.py::test_research_plan_timeline_refresh_uses_artifact_locale_when_query_locale_absent apps/backend/tests/test_api_flow.py::test_research_plan_timeline_defaults_to_latest_project_locale -q`, `.venv/bin/pytest apps/backend/tests/test_api_flow.py::test_public_benchmark_workflow_runs_baseline_and_reports apps/backend/tests/test_api_flow.py::test_public_benchmark_workflow_rejects_credentialed_source -q`, `npm --prefix apps/frontend run build`, and `git diff --check`.
  - Benchmark/relational helper queueing follow-up: `.venv/bin/python -m py_compile apps/backend/tabular_harness/worker/jobs.py apps/backend/tabular_harness/api/routes.py apps/backend/tests/test_api_flow.py`, `.venv/bin/ruff check apps/backend/tabular_harness/worker/jobs.py apps/backend/tabular_harness/api/routes.py apps/backend/tests/test_api_flow.py`, `.venv/bin/pytest apps/backend/tests/test_api_flow.py::test_benchmark_catalog_and_local_import apps/backend/tests/test_api_flow.py::test_benchmark_evidence_pack_empty_project apps/backend/tests/test_api_flow.py::test_home_credit_fixture_smoke_harness apps/backend/tests/test_api_flow.py::test_benchmark_relational_catalog_infers_shared_keys -q`, and `git diff --check`.
  - Benchmark import/fixture queueing follow-up: `.venv/bin/python -m py_compile apps/backend/tabular_harness/worker/jobs.py apps/backend/tabular_harness/api/routes.py apps/backend/tests/test_api_flow.py`, `.venv/bin/ruff check apps/backend/tabular_harness/worker/jobs.py apps/backend/tabular_harness/api/routes.py apps/backend/tests/test_api_flow.py`, `.venv/bin/pytest apps/backend/tests/test_api_flow.py::test_benchmark_catalog_and_local_import apps/backend/tests/test_api_flow.py::test_public_uci_wine_fixture_source_card_import apps/backend/tests/test_api_flow.py::test_home_credit_fixture_smoke_harness apps/backend/tests/test_api_flow.py::test_benchmark_relational_catalog_infers_shared_keys -q`, `.venv/bin/pytest apps/backend/tests/test_api_flow.py -q`, `.venv/bin/pytest apps/backend/tests/test_worker_daemon.py -q`, and `git diff --check`.
  - Guided Journey queueing follow-up: `.venv/bin/python -m py_compile apps/backend/tabular_harness/worker/jobs.py apps/backend/tabular_harness/api/routes.py apps/backend/tests/test_api_flow.py`, `.venv/bin/ruff check apps/backend/tabular_harness/worker/jobs.py apps/backend/tabular_harness/api/routes.py apps/backend/tests/test_api_flow.py`, `.venv/bin/pytest apps/backend/tests/test_api_flow.py::test_project_guidance_recommends_next_focus -q`, `.venv/bin/pytest apps/backend/tests/test_api_flow.py -q`, `.venv/bin/pytest apps/backend/tests/test_worker_daemon.py -q`, and `git diff --check`.
  - Relational schema hint upload queueing follow-up: `.venv/bin/python -m py_compile apps/backend/tabular_harness/worker/jobs.py apps/backend/tabular_harness/api/routes.py apps/backend/tests/test_api_flow.py`, `.venv/bin/ruff check apps/backend/tabular_harness/worker/jobs.py apps/backend/tabular_harness/api/routes.py apps/backend/tests/test_api_flow.py`, `.venv/bin/pytest apps/backend/tests/test_api_flow.py::test_relational_schema_hint_upload_preview_and_agent_route -q`, `.venv/bin/pytest apps/backend/tests/test_api_flow.py -q`, `.venv/bin/pytest apps/backend/tests/test_worker_daemon.py -q`, and `git diff --check`.
